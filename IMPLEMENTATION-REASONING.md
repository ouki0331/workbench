# 第一阶段执行思路与判断依据

更新时间：2026-09-16

## 1. 为什么先做资料管理

练习、复习、搜索、统计和学习计划都需要引用同一份文章、音频、录音或词汇。如果资料没有稳定身份，后面每个模块都会各自保存标题和文件路径，修改标题、删除资料或更换存储时容易断开。因此第一阶段先建立“资源、文件、分类、笔记、浏览记录”的共同地基，再增加新的学习功能。

当前本机没有必须迁移的高价值资料，所以不做旧文件回填和双写。新资料直接进入 SQLite；二进制文件仍保存到资料目录。这样省去兼容层，也避免为了测试数据长期保留两套事实来源。

## 2. 一篇文章为什么会得到某个标签

自动标签的目标是减少录入工作，不是代替用户作最终判断。每个标签关联都保存 `assignment_source` 和可选 `confidence`，界面能说明它来自哪里。

### 2.1 证据优先级

| 等级 | 依据 | 示例 | 可靠性与处理 |
| --- | --- | --- | --- |
| A | 用户明确选择或修改 | 用户选择“金融”“英国” | 最高；后续自动分析不得覆盖 |
| A | 文件或来源的确定元数据 | MP3 内嵌时长、HTML language、MIME 类型 | 对相应字段可靠；保存原始值和提取时间 |
| B | 外部受控分类的直接映射 | 来源 API 返回 IPTC Media Topic ID | 映射可靠，内容本身是否分类正确仍取决于来源 |
| B | 导入上下文的确定规则 | 从阅读练习保存的文章拥有“阅读”技能 | 可靠，但只说明使用方式，不说明内容主题 |
| C | 本机规则和关键词候选 | 高频出现 central bank、interest rate | 只作为候选；默认要求确认 |
| D | AI 模型分类 | 模型根据全文建议“经济、金融” | 显示模型、日期和可信度；不自动覆盖人工标签 |

第一版使用 A、B 和少量 C。暂不为了自动打标签调用付费 AI。

### 2.2 各分类的具体依据

**内容类型**主要依据导入入口、MIME 类型和文档结构：音频文件可确定为音频；带问题结构的 JSON 可确定为题组；网页正文通常先建议文章，只有来源元数据和结构明确时才建议新闻报道。文件扩展名只能作为辅助，不能把所有 HTML 都标成新闻。

**技能**表示“可以怎样学习”，不是文章主题。通过阅读入口导入的文章建议阅读；配套音频存在时建议听力；用户把材料用于写作题目后再增加写作关系。一个材料可以同时服务多个技能。

**主题**依据用户选择、来源受控分类或正文内容。规则候选至少需要多个独立信号，例如标题和正文都出现中央银行、利率或通胀，才建议“金融/经济”。仅出现一次 `bank` 不能判断为金融，因为它也可能表示河岸。

**体裁**依据表达目的和结构：报道近期事件为新闻；提出判断并论证为评论；解释概念或背景为解读；问答结构为访谈；虚构人物和情节再区分短篇小说、长篇小说。系统无法确定时保持空白。

**地区**只有在地区是内容讨论对象时才加入 `coverage`。文章提到“Tokyo”一次不等于主题涉及日本；标题、摘要、主体段落或来源分类需要表明该地点具有实质关系。资料来源地 `origin` 放在以后完成。

**难度**需要词汇、句法和篇章信号共同判断。第一版优先采用有来源的 CEFR 数据和用户修正；本机规则产生的等级显示“估计”。难度不是考试分数。

### 2.3 自动标签交互

1. 导入完成后显示“系统建议”，按证据等级排序。
2. 确定信息直接填入，但仍可修改；推测信息保持未选中或显示待确认。
3. 用户逐项接受、删除或修改后保存。
4. 人工修改写入来源，后续重新分析不覆盖。
5. 详情页可以查看标签依据，例如“来源 API：IPTC medtop:04000000”或“正文关键词候选”。

这套方式的可靠性边界很明确：文件时长等技术元数据通常可靠；主题和难度属于分类判断，只能给候选和依据。产品不能只显示一个标签而隐藏产生过程。

## 3. 元数据提取思路

### 音频和录音

读取容器信息得到时长、编码、采样率、声道、内嵌标题、艺术家、专辑和录制日期。标题优先级为：用户输入 > 内嵌标题 > 清理后的文件名。时长保存自动值和来源；人工修正后不再被重新扫描覆盖。

### 网页

先保存原始 URL 和抓取时间，再读取 HTML title、Open Graph、JSON-LD、作者、发布时间、站点名和语言。正文抽取使用 Mozilla Readability 作为首选候选；完整页面快照另行评估 SingleFile。自动提取失败时保留 URL 和原始 HTML，不生成看似完整的空文章。

### PDF 与 EPUB

先读标准文档元数据和目录结构。扫描 PDF 可能没有可选文字，应标记“需要 OCR”，不把空抽取误认为成功。EPUB 以书和章节建立关系，而不是把整本书塞成一篇文章。

## 4. 浏览时间为什么分两种

用户重视时间，但网页打开时长不等于学习时间。因此同时保留：

- **停留时间**：页面打开至离开的跨度，用于回顾浏览行为。
- **活跃时间**：页面可见且近期有交互的时间，用于学习周报。
- **实际播放时间**：播放器真正播放的秒数，用于听力统计。
- **录音时间**：录音媒体本身的实际时长，用于口语统计。
- **最后查看时间**：最近一次有效活动的时间，用于排序和提醒。

统计报表默认使用活跃时间。详情页可以同时展示停留时间，避免用户觉得数据被隐藏。

## 5. 为什么备注和笔记分开

备注描述资源本身，例如“来源页面缺少图表”“从老师处取得”“有效音频到 17:20”，所以每份资料一个即可。笔记表达用户在不同时间产生的理解和感悟，可能持续增加，因此独立成多条 Markdown 文档。两者分别记录修改时间，资料标题修改也不能伪装成笔记更新时间。

## 6. 如何支持 IELTS、会计学和经济学

核心表使用通用名 `resources`、`resource_notes`、`practice_attempts`，不加 `ielts_`、`accounting_` 或 `economics_` 前缀。业务归属通过三层关系表达：

```text
用户 → 学习模块 → 科目 → 资料/练习/计划
```

当前模块是 IELTS，科目包括听力、阅读、写作、口语、词汇和语法。以后可以新增“会计学”模块，科目可以是财务会计、管理会计、审计和税务；“经济学”模块可以包含微观经济学、宏观经济学和计量经济学。相同的金融文章可以同时关联 IELTS 阅读和经济学参考资料，无需复制文件。

只有会计特有的数据才建专门扩展表，例如分录练习、账户科目或报表模板。是否需要这些表，应等会计学习流程明确后决定。给通用表加学科前缀会造成重复表和跨科目搜索困难，因此不采用。

所有个人数据表预留 `user_id` 或通过所属资源追溯用户。当前只有一个本地默认用户；以后开放账号时替换身份认证，不需要给每张表重新改变业务含义。

## 7. 第一阶段执行顺序

1. 完成通用用户、模块、科目、备注、笔记、时长和浏览会话表。
2. 实现资料管理独立页面和新资料直接写入数据库。
3. 实现确定性元数据提取，并明确显示来源和失败状态。
4. 实现人工编辑、标签候选确认和多篇 Markdown 笔记。
5. 实现活跃时间、停留时间和最后查看时间。
6. 实现归档、回收站、去重和组合筛选。
7. 用 20 份真实混合资料验证字段；再用 100 份数据验证检索。
8. 把实际发现的字段冲突、误分类和无用标签回写本文件及待确认清单。

## 8. 重构时需要保留的原则

- 稳定机器 ID 与多语言显示名称分开。
- 人工值的优先级高于自动推断。
- 保存推断依据、来源和时间，不只保存结论。
- 原始文件和可检索正文分开保存，并保持关系。
- 时间统计说明计算口径。
- 学科差异通过模块和扩展表表达，不复制整套通用表。
- 新存储或新模型通过接口替换，资源 ID 和学习历史保持稳定。

