# Known Issues

## DEV-004：自定义标签无法从资料详情创建

状态：已记录，当前阶段不修复
发现日期：2026-09-20

在本机 SQLite 预览中打开资料详情后，系统主题、技能、体裁等标签可以选择并保存，但自定义标签无法完成创建。因此，同名系统标签冲突提示也无法通过界面继续验证。

当前影响限于创建新的个人标签；备注、笔记、系统标签、地区选择和建议功能仍可使用。后续迭代应检查自定义标签名称、颜色选择、创建动作及保存请求之间的连接，并补一条浏览器人工用例。

建议验收：

1. 输入一个未使用名称并选择 Sakura、Navy、Olive 或 Charcoal 后能够创建并保存。
2. 重开资料后自定义标签仍被选中，名称和颜色保持。
3. 输入与系统标签相同或规范化后冲突的名称时显示可修正错误，表单内容不丢失。
4. 停用自定义标签后不再用于新分配，已有资料关系仍可显示。
