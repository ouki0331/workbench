# 口语发音与自动评分评估

更新时间：2026-09-16

## 1. 现代系统如何评分

现代语音学习产品通常组合四层能力，并非只把录音发给聊天模型：

1. **语音转文字（ASR）**：得到转写、单词时间点和识别可信度。
2. **对齐与发音评分**：朗读已知文本时，把录音和参考文本对齐到单词、音节或音素，判断遗漏、插入和误读。
3. **流利度与韵律分析**：计算语速、停顿、重复、节奏、重音、音高变化和语调。
4. **内容评分**：把转写文本交给规则或语言模型，按照任务回应、连贯、词汇和语法量表评价。

“跟 AI 自由聊天”一般还会加语音活动检测、实时 ASR、对话模型和 TTS。对话模型负责内容反馈；发音准确度通常来自专门的语音评估模型。

## 2. 两种练习的难度不同

### 跟读或朗读

系统已经知道正确文本，能做强制对齐，较容易指出哪个词或音素不准确。适合第一版发音功能。

### 自由回答

系统不知道用户本来要说什么。ASR 识别错和用户发音错可能混在一起；口音、麦克风和环境噪声也会影响结果。内容、语法和流利度可以评估，但细粒度音素评分的可信度低于朗读模式。

## 3. 可选方案

### 方案 A：本机低成本指标

实现录音回听、总时长、有效说话时长、语速、长停顿数量和手工自评。语音活动检测可以在本机完成。

优点：没有账号和按量费用，录音不离开电脑。限制：不能可靠判断单个音素是否发准，也不能给出可信 IELTS 分数。

### 方案 B：Azure Pronunciation Assessment

Azure Speech 提供朗读和自由回答评估。基础结果包括 accuracy、fluency、completeness 和 miscue；prosody 以及部分自由回答内容指标属于增强能力。官方说明基础发音评估按标准 Speech to Text 计费，prosody 等功能可能额外收费。

需要：Microsoft Azure 账号、Speech 资源、区域和 API 密钥。录音需要发送到 Azure。价格随区域和计费层变化，实施前在官方价格页按日本区域重新确认。

适合：想先做可靠的朗读发音训练，并需要单词/音素级反馈。

### 方案 C：Speechace

Speechace 是专门面向语言学习的商业 API。当前公开价格中，基础发音方案约 40 美元/月，朗读流利度约 80 美元/月，自由回答和综合口语约 125 美元/月，并有调用量限制和超额费用。

适合：需要接近成品教育产品的评分维度，并愿意承担固定月费。对当前单用户工作台成本偏高。

### 方案 D：ASR + 通用大模型

使用 Whisper 或其他 ASR 得到转写，再让 OpenAI 或 Claude 按自定义量表评价回答内容、语法、词汇和连贯。它适合内容反馈，不应单独用于音素级发音准确度。还需要另外计算停顿和语速。

需要：ASR 和模型 API 账号及按量费用。政治主题材料优先选择用户信任的数据处理区域和供应商，并在发送前明确显示录音或转写会离开本机。

## 4. 推荐路线

1. **现在**：保留录音、回听、手工标记时间点、自评；增加本机语速和长停顿等简单指标。
2. **小规模试验**：增加“朗读模式”，使用 Azure 免费额度或少量付费请求测试单词、音素、流利度反馈是否真的有帮助。
3. **自由回答**：只有朗读试验有价值后，再增加 ASR 转写和语言模型内容反馈。
4. **正式评分**：收集人工复核样本后才显示趋势；不把供应商分数直接当作官方 IELTS Speaking Band。

这个顺序可以把高成本的自由回答评分推迟，同时先解决“哪个词读得不准”和“停顿是不是太多”两个具体问题。

## 5. 数据设计

未来建议增加：

- `speech_assessments`：录音、模式、提供商、模型版本、语言、参考文本、创建时间、费用估计。
- `speech_assessment_scores`：accuracy、fluency、completeness、prosody、content 等维度和量表版本。
- `speech_word_scores`：单词、开始/结束时间、错误类型、分数。
- `speech_phoneme_scores`：音素、期望值、识别值和分数，仅对支持的提供商保存。
- `speech_feedback_items`：用户选择保留的建议，可转成复习卡。

原始供应商 JSON 应单独保存并带版本，界面使用统一内部字段。以后更换 Azure、Speechace 或其他服务时，历史结果仍能解释。

## 6. 用例

**用例 A：朗读纠音**

用户朗读一段已知文本。系统对齐参考文本，显示 `economic` 的重音或音素准确度偏低，并允许用户播放自己的片段和标准示例。用户把该词加入发音复习卡。

**用例 B：自由回答**

用户回答 IELTS Part 2 两分钟。系统先转写，再显示实际说话时间、长停顿、语速和重复词；语言模型只评价内容与表达。页面明确区分“发音服务评分”“文本内容反馈”和“非官方 IELTS 估计”。

## 7. 参考资料

- Azure Pronunciation Assessment：<https://learn.microsoft.com/en-us/azure/ai-services/speech-service/pronunciation-assessment-tool>
- Azure Speech 定价：<https://azure.microsoft.com/pricing/details/speech/>
- Speechace API 方案：<https://www.speechace.com/api-plans/>

