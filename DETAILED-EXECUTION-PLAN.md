# 详细执行计划

更新时间：2026-09-16

> 当前交付范围、任务状态和下一任务以第 13 节为准。第 1–12 节保留历史编号与详细用例；其中阶段 5–7、批次数及串行人工等待不构成本轮承诺。产品事实仍以能力地图及已确认决定为准。

## 1. 今后的任务说明格式

每个业务任务交给 agent 前必须包含以下内容：

1. **用户问题**：为什么需要这个功能。
2. **本次范围**：本次明确完成什么。
3. **暂不包含**：防止 agent 自行扩大范围。
4. **用户流程**：从哪个页面进入、依次做什么、完成后看到什么。
5. **数据与关系**：新增或修改哪些字段，关联什么资料。
6. **业务规则**：默认值、限制、冲突、失败和空状态。
7. **界面状态**：正常、加载、空、失败、只读、归档或回收站状态。
8. **验收标准**：用户可以直接检查的结果。
9. **用例**：至少 1 个正常用例和 1 个边界用例。
10. **待确认项**：编号写入 `DECISIONS-NEEDED.md`，不得散落在聊天或代码注释中。

任务完成后，agent 需要回写：实际完成范围、与计划的差异、验证结果、新增待确认项和下一步依赖。

## 2. 阶段总表

| 阶段 | 目标 | 到什么程度算合适 | 预计批次 | 依赖 |
| --- | --- | --- | ---: | --- |
| 0 | 数据地基 | SQLite 可重复初始化，关系图、标签字典、迁移规则可审查；现有页面不受影响 | 1 | 无 |
| 1 | 资料可管理 | 100 份混合资料仍能编辑、过滤、归档、恢复、识别重复和查看关联 | 4 | 阶段 0 |
| 2 | 主观练习进入复习 | 写作、口语、文章重点句都能手动变成有来源的复习卡 | 2 | 阶段 1 关系模型 |
| 3 | 无 AI 的有效反馈 | 每次写作/口语完成结构化自评，并把问题带到下次练习 | 2 | 阶段 2 |
| 4 | 统计指导行动 | 首页给出少量有依据的下一步；规则可解释、可忽略 | 2 | 阶段 2–3 数据 |
| 5 | 计划与完成感 | Markdown 计划可预览修正，任务可完成/跳过/顺延，里程碑来自真实行为 | 3 | 标签与关系稳定 |
| 6 | 题库与能力趋势 | 至少一个真实题库流程可用，多组可比较短测形成趋势 | 3–5 | 数据源调查 |
| 7 | 按需增强 | 写作 AI、语法解释、EPUB、ASR/TTS、Cloudflare | 分项决定 | 触发条件 |

“批次”表示一次适合独立实现和验收的 agent 任务，不代表自然日。

## 3. 阶段 0：数据库和分类地基

### 0.1 SQLite 初始化

**用户问题**：文件目录适合保存原始资料，但编辑、关联、组合筛选、去重和统计会越来越困难。

**本次范围**：创建 schema migration、用户占位、学习模块/科目、核心资料表、文件表、关系表、标签表、地区表、单一备注、多篇笔记、浏览会话和全文索引；提供幂等初始化命令；实际生成本机数据库。

**暂不包含**：不把现有文章或浏览器进度迁入数据库，不改页面读取来源。

**用户流程**：无新增页面。开发或迁移 agent 执行 `npm run db:init`，得到数据库路径、迁移数、分类数和地区数。

**业务规则**：数据库保存在资料目录；大文件不写 BLOB；外键开启；迁移只能向前追加；初始化可重复执行；当前只有一个本地默认用户，但所有个人学习数据按用户归属；表名保持学科中立。

**验收标准**：连续执行两次不报错、不重复数据；数据库完整性检查通过；现有网站仍能启动和读取原文件。

**用例 A**：全新电脑复制项目和资料目录后执行初始化，得到同样的核心分类。

**用例 B**：数据库已存在并执行过 `001`，再次运行只补种缺失的系统标签，不清除个人数据。

### 0.2 标签目录

完整标签组、颜色、用途、边界和词汇等级约束见 `TAG-TAXONOMY.md`。

**本次范围**：固定 7 个分类组、17 个主题、16 个体裁、四语言名称、外部标准映射、统一色组和停用机制。

**暂不包含**：不导入完整 IPTC 树，不自动判断文章政治立场，不声称存在官方 IELTS/TOEFL 逐词等级。

**验收标准**：agent 能仅根据标签文档判断“科技新闻”应如何拆分；四种界面语言切换时稳定使用同一 `key`；系统标签停用后历史关系仍保留。

**用例 A**：英国央行文章标记为文章、阅读、经济、金融、分析、英国、IELTS、B2 估计。

**用例 B**：用户建立“周末精读”，发现已有“周末阅读”时系统提示合并或继续新建，不自动合并。

## 4. 阶段 1：资料管理

### 1.1 已取消：旧资料回填与双写

当前本机没有需要保留的高价值资料，因此不投入成本扫描旧目录、双写旧格式或做迁移对账。阶段 1 直接让新资料写入 SQLite 和资料文件目录。旧测试资料可以保留在原目录外作为样例，或在启用新页面前清空。

将来从 SQLite 迁移到 PostgreSQL 时重新制定独立迁移方案，届时根据真实数据量处理导出、校验、停机窗口和回滚，不沿用本阶段的旧文件兼容策略。

### 1.2 资料管理独立页面与编辑

**用户问题**：资料的字段、文件信息、标签、备注和多篇笔记较多，塞在导入弹窗或练习页面中会难以查看和修改。

**本次范围**：新增独立“资料管理”页面。页面包含资料列表、组合筛选、资料详情、编辑表单、单一备注和多个 Markdown 笔记。可编辑标题、摘要、来源 URL、内容类型、技能、主题、体裁、地区、考试关联、建议难度和自定义标签。正文编辑只对 TXT/Markdown 开放；PDF、音频和录音只编辑元数据。

**导入时自动提取**：

- 音频和录音：读取容器时长、MIME 类型、文件大小；有内嵌标签时读取标题、艺术家、专辑和录制日期作为候选值。
- 网页：读取 HTML title、Open Graph 标题、作者、发布时间、站点名、语言和摘要作为候选值。
- PDF/EPUB：读取文档标题、作者、语言、创建时间、章节信息；缺失时使用文件名作为候选标题。
- 自动提取结果记录来源和提取时间。用户修改后字段来源变为“人工”，后续重新提取不能覆盖人工值。
- 自动提取器的具体库和格式兼容性作为实现任务继续评估，见待确认清单 D-013。

**备注与笔记**：

- 每份资料有一个备注，使用 Markdown 保存，适合写文件说明、来源说明或使用限制；记录 `remark_updated_at`。
- 每份资料可有多篇笔记。每篇笔记有标题、Markdown 正文、创建时间、修改时间和排序值。
- 笔记适合保存个人理解、摘要、问题和学习感悟；笔记进入统一搜索。
- 资料修改时间、备注修改时间和每篇笔记修改时间分别显示，不能混成一个时间。

**用户流程**：资料管理 → 选择资料 → 查看自动提取信息和来源 → 编辑字段/标签 → 保存。备注在详情页直接编辑；笔记可以新建、改名、排序、编辑和删除。

**业务规则**：标题不能为空；自动提取失败时使用文件名并显示“部分信息未识别”；音频时长自动识别后允许手工修正，手工值必须标记来源；URL 可为空但格式需有效；内容类型必填；其他分类可未知；离开未保存的备注或笔记时提示。

**验收标准**：录入常见 MP3/M4A/WAV/WebM 后显示时长且允许修正；保存后重新启动仍能看到修改；一份资料可以建立至少 20 篇独立笔记；Markdown 可预览并能全文检索；四类修改时间准确区分；失败时保留表单内容。

**用例 A**：用户导入一个带标题标签的 18 分钟 MP3。系统自动填入标题和 `18:03`，用户发现结尾静音不应计入学习长度，把有效时长改为 `17:20`，页面显示“人工修正”。随后新增“第一遍听写”和“第二遍复盘”两篇笔记。

**用例 B**：用户导入没有内嵌信息的录音，系统使用文件名并识别时长。识别时长失败时字段保持未知，用户可以手工填写；失败不会阻止文件保存。

### 1.3 收藏、归档、回收站与恢复

**本次范围**：收藏切换；归档和取消归档；移入回收站；从回收站恢复；查看删除影响。彻底删除入口放回收站详情，第一版不自动清空。

**业务规则**：归档资料仍可搜索但默认隐藏；回收站资料不进入推荐和今日计划；恢复后回到原标签和关系；彻底删除前列出关联练习、复习卡和文件数量。

**验收标准**：误删资料可完整恢复；归档不会改变学习历史；收藏可作为搜索条件；彻底删除不会留下失效文件引用。

**用例 A**：完成一篇旧文章后归档，搜索选择“包括归档”仍可找到并查看练习历史。

**用例 B**：删除带录音和 5 张复习卡的口语任务时，影响预览明确列出这些关系，用户取消后没有任何变化。

### 1.4 重复提示

**本次范围**：按规范化 URL、二进制 SHA-256、规范化正文哈希三个强信号提示；标题和标签相似仅作弱提示。用户可以打开已有资料、保留为独立版本或标记为重复。

**业务规则**：重复提示不自动删除；相同 URL 但内容更新时间不同可保存为新版本；同标题不是重复依据；个人录音即使时长相同也不能仅按时长判重。

**验收标准**：重复导入同一文件时先看到已有项；选择保留新版后两项关系可追踪；选择取消不会产生临时残留。

**用例 A**：再次导入相同 MP3，系统显示文件完全相同和已有练习引用。

**用例 B**：同一新闻 URL 更新正文后，用户选择保存为新版本并关联旧版本。

### 1.5 筛选、搜索和保存视图

**本次范围**：类型、技能、主题、体裁、地区、考试、难度、收藏、状态和日期组合筛选；关键词覆盖标题、正文、摘要、备注和多篇笔记；结果显示关键标签和关系数量。第二小批增加“保存当前筛选”。同时记录资料页面停留时长、活跃阅读时长和最后查看时间。

**浏览时间规则**：

- `dwell_seconds` 记录资料页面从打开到离开的总停留时间。
- `active_seconds` 只累计页面可见且用户最近 60 秒内有滚动、点击、键盘、播放或选词等活动的时间，避免把隔夜未关闭的标签页算成学习。
- `last_viewed_at` 取最近一次浏览会话的最后活动时间。
- 列表显示累计活跃时间和最后查看时间；详情页可以查看每次浏览记录。
- 音频播放时间继续按播放器实际播放秒数统计，不能用页面停留时间代替。

**业务规则**：同组多选默认 OR，不同组之间 AND；清除按钮恢复默认；筛选写入 URL 或页面状态以便返回；全文结果高亮上下文；音频无转写时只搜索元数据。浏览会话异常关闭时以最后一次心跳结算，不把之后时间补算进去。

**验收标准**：100 份资料时能在三次筛选内找到目标；Ctrl/⌘+K 和资料库使用同一筛选语义；结果为空时显示当前条件并可逐项移除；页面前台阅读 5 分钟后累计时间接近 5 分钟；切到其他标签页 5 分钟不会继续累计活跃时间；最后查看时间可用于排序。

**用例 A**：查找“上个月 + 阅读 + 金融 + 英国 + 未归档”的资料。

**用例 B**：搜索录音内容但没有转写，结果明确说明当前只匹配标题和备注，并提供按口语录音筛选。

**用例 C**：用户打开文章 3 分钟后切到其他标签页 20 分钟，再回来阅读 2 分钟。总停留会话保留真实跨度，学习统计只增加约 5 分钟活跃阅读时间。

### 1.6 待办：资料关系详情、网页本地保存和失效检查

该任务不放进最近一轮实现。未来详情页显示“原始材料、配套音频、练习记录、从中收藏的单词、复习卡、计划任务”。每种关系显示数量和最近一项，可展开。

网页保存优先采用两层结果：一份适合练习和全文检索的正文 Markdown/清理 HTML，以及一份尽可能保持原貌的离线 HTML 快照。正文提取优先评估 Mozilla Readability；完整页面快照评估 SingleFile。动态播放器、登录内容和复杂交互可能无法完全离线重现，保存结果必须显示完整性状态。

若网页无法本地保存，只保留外部 URL，则进入失效检查队列：距离上次检查满 30 天且用户再次打开资料管理页时安排检查；记录 HTTP 状态、最终 URL、检查时间和连续失败次数。检查结果集中显示在“链接健康”页面，不在第一版发送通知。

后续可改善：连续失败达到阈值后发送站内提醒；邮件提醒等到用户启用账号和邮件渠道后再评估。

**业务规则**：关系必须有明确类型；手动解除关系不删除两端资源；本地副本存在时优先打开本地内容；外链失效不影响本地副本；一次网络超时不直接判定永久失效；失效文件显示“需要修复”，不让页面整体失败。

**验收标准**：从文章可进入对应练习和复习卡，从复习卡可返回文章上下文；关联数量与数据库一致；无法离线保存的链接能显示最近检查结果；本地快照存在时原站失效仍可阅读。

**用例 A**：金融文章详情显示 1 个音频、2 次练习、12 个单词和 3 张重点句卡。

**用例 B**：配套音频文件被外部移动后，文章仍能打开，并显示修复文件路径的操作。

**用例 C**：某动态网页无法完整保存，只留下正文和外链。一个月后检查连续失败，链接健康页显示问题；已保存正文仍可练习，不发送邮件。

## 5. 阶段 2：主观练习进入复习

### 2.1 重点句卡

**本次范围**：听力/阅读正文选中一句或短语 → “加入复习” → 编辑卡片正面、答案、解释和上下文 → 保存。卡片保留文章、段落和选区位置。

**业务规则**：选择过长时提示缩短；重复选区提示已有卡；原文以后编辑时保留当时上下文快照，并尽力重新定位。

**验收标准**：复习页面可答卡并返回原文；来源资料归档后卡仍可复习；来源进入回收站时卡显示来源状态。

**用例 A**：用户保存一个不理解的条件句，背面写中文解释，第二天复习后返回原段落。

**用例 B**：同一句再次收藏时打开已有卡，而不是静默生成重复卡。

### 2.2 写作问题卡和口语表达卡

**本次范围**：写作中标记“需要重写”“以后要使用”的表达；口语回听后手工输入“原表达、目标表达、备注”，可关联录音时间点；保存为复习卡。

**业务规则**：没有自动转写时不假装识别录音内容；录音时间点可选；用户在保存前可编辑两面；卡片可在下次同技能练习前集中预习。

**验收标准**：每次写作或口语可以留下至少一个明确改进点；复习完成状态进入周报；能打开原回答或播放关联录音位置。

**用例 A**：写作完成后把重复使用的 `important` 标为需要重写，目标表达写为两个替换方案。

**用例 B**：用户只记得口语第 48 秒处停顿，可以保存时间点和下次要说的句子，不要求上传新文件。

发音准确度、流利度和自动口语评分属于独立后期能力，不能由普通录音播放器或大语言模型单独可靠完成。实现方式、商业服务、成本和推荐的分阶段方案见 `SPEAKING-ASSESSMENT-OPTIONS.md`。

## 6. 阶段 3：结构化自评

### 3.1 写作自评

**本次范围**：任务回应、结构连贯、词汇、语法四组，每组 2–4 个可回答问题；答案为“做到/部分做到/未做到/不适用”，附可选备注。显示最近同类型练习的变化。

示例问题：是否覆盖题目要求的每个部分；每段是否有明确中心；是否重复依赖少数常用词；是否检查主谓一致和句子边界。

**业务规则**：不要求用户给自己打 IELTS Band；不同写作任务类型使用对应清单；“未做到”可一键建立复习卡或下次任务。

**验收标准**：完成后自动形成最多 2 个下次行动；历史页能比较相同指标；没有填写的项不算失败。

**用例 A**：Task 2 自评发现例证不足，生成“下一篇每个主体段至少一个具体例证”的行动。

**用例 B**：用户中途保存草稿，系统保存自评草稿但不计入完成趋势。

### 3.2 口语自评

**本次范围**：回答完整度、流利度、词汇、语法、发音感受五组；允许播放本次录音；记录一个做得好的点和一个要改的点。

**业务规则**：自我感受不等于客观发音分数；无录音也可自评但标记证据不足；同一问题连续出现时供规则建议使用。

**验收标准**：10 分钟以内完成练习和自评；改进点可以成为复习卡或计划任务；历史比较不显示虚假精确分数。

**用例 A**：用户回听后标记“停顿过多”，并加入下次 2 分钟不停顿练习。

**用例 B**：麦克风权限失败，用户仍可完成文本自评，记录中明确没有录音证据。

## 7. 阶段 4：让统计产生下一步

### 4.1 首页信号

**本次范围**：今日到期复习数、最近最少练习技能、上次未解决问题、继续最近材料。每项直接进入对应动作。

**业务规则**：没有足够数据时显示新手建议；只显示一个主要行动和最多两个次要行动；已在当天完成的技能不继续催促。

**验收标准**：打开首页 10 秒内能开始一个合理任务；建议展示依据日期或数量；点击后到达可执行页面。

**用例 A**：10 天无口语且有到期表达卡，主要行动为 10 分钟口语并先复习 3 张相关卡。

**用例 B**：新用户没有记录，首页建议完成快速摸底或选择一篇内置材料，不显示“数据不足”死页面。

### 4.2 规则提示

第一批只启用四条规则：

1. 某技能 7 天没有练习。
2. 到期复习超过个人近 14 天日均完成量的两倍，且至少 10 张。
3. 最近 14 天四项学习时长中一项低于 10%。
4. 最近 3 次同类自评重复出现同一“未做到”。

**业务规则**：一次最多显示两条；每条包含证据、建议动作、“今天忽略”和“7 天内不再提示”；规则阈值以后可设置。

**验收标准**：相同条件不会在每次导航时重复弹出；完成建议动作后提示自动解决；数据变化能重新计算。

**用例 A**：阅读占 70%、口语为 0%，提示安排一次口语，不评价用户能力。

**用例 B**：用户处于旅行周，选择 7 天内不再提示；学习记录仍照常保存。

## 8. 阶段 5：计划和完成感

### 5.1 Markdown 计划导入

**本次范围**：支持约定标题、日期、任务、时长、技能和标签的 Markdown；导入后先显示结构化预览；未知字段和无法解析行单独列出；用户修正后才保存。

**业务规则**：AI 只负责生成 Markdown 初稿；工作台决定数据是否合法。计划任务可以引用固定资料或标签查询。重复导入显示差异，不静默复制。

**验收标准**：部分错误的计划仍可导入有效任务；预览中能改日期和标签；导出再导入保持任务信息。

**用例 A**：用户把 AI 生成的 14 天计划粘贴进来，两个未知主题被标红，修正后得到每天任务。

**用例 B**：计划写“完成一篇 B2 科技阅读”但没指定文章，执行当天从符合条件且未使用的资源中选择。

### 5.2 任务状态与里程碑

**本次范围**：完成、跳过、顺延、改期；每周目标完成；主题听说读写循环；连续四周保持四项覆盖；用户自定数量目标。

**业务规则**：跳过不算失败；顺延保留原计划日期；里程碑不锁主题、不制造惩罚；统计根据真实 activity/attempt/review 事件计算。

**验收标准**：计划变更有历史；里程碑可以点开查看构成证据；撤销误记完成后结果同步回退。

**用例 A**：本周完成经济主题听说读写各一次，获得主题循环完成记录并查看四次练习。

**用例 B**：周三任务顺延到周五，周三显示已顺延而非未完成，周五不会产生重复任务。

## 9. 阶段 6：题库与能力趋势

### 6.1 数据源调查闸门

在开发题库导入前，用一个独立调研任务回答：许可是否允许保存和二次使用、实际格式、题型、答案和解析覆盖、更新频率、是否需要账号/API 密钥。只有找到一个真实可用来源，或确认“用户自建 + AI 生成后导入”本身足够，才进入实现。

### 6.2 题库第一版

**本次范围**：JSON/CSV 导入与导出；单选、多选、判断；题目编辑；答题、判定、解析、错题卡。填空作为下一小批。

**验收标准**：至少一个 50 题真实数据包可往返导入导出；错误行有定位；答案修改后新练习使用新版，历史作答保留原版本。

**用例 A**：导入 30 道单选题，2 行缺少正确答案，预览允许修正后导入其余题目。

**用例 B**：题目答案后来修正，过去一次练习保留当时判定，并显示答案版本已更新。

### 6.3 能力趋势

**本次范围**：准备至少 3 组结构和难度相近的短测；显示正确率区间、技能/题型差异和相对上次变化；不显示官方 Band。

**验收标准**：趋势不重复同一组题；不同卷的题型构成可比；题量太少时只显示观察，不给精确结论。

**用例 A**：两周后完成等量 B 卷，阅读定位题由 40% 到 70%，显示改善和样本数。

**用例 B**：用户只完成半套，结果标为未完成，不进入正式趋势线。

## 10. 阶段 7：有触发条件才开始

| 功能 | 开始条件 | 第一版边界 |
| --- | --- | --- |
| 写作 AI | 结构化自评持续使用，用户确认 API 费用 | 结构、语法、表达建议；固定 JSON；可选建议进复习；不冒充官方 Band |
| 语法解释 | 用户频繁收藏完整句子，愿意使用模型 | 选中后调用、四语言解释、缓存、显示来源和不确定性 |
| EPUB | 至少有一本真实电子书需要导入 | 目录预览、章节选择、正文与元数据；浏览器朗读 |
| ASR/口语 AI | 口语录音持续使用且手动记录成为负担 | 先转写，再做内容/流利度反馈；发音细分最后考虑 |
| 云端 TTS | 浏览器朗读无法满足保存或声音一致性 | 明确声音、费用、缓存和版权范围 |
| Cloudflare | 明确需要第二设备或远程访问 | 单用户认证、D1 元数据、R2 文件、完整迁移和回滚 |
| 通用学习模块 | 有第二个真实学习场景 | 从两个场景提炼共同数据模型，不提前做空泛抽象 |

## 11. 建议的最近 8 个 agent 任务

| 顺序 | 可独立交付任务 | 输出 | 验收后才进入 |
| ---: | --- | --- | --- |
| 1 | 数据库地基 | migration、seed、初始化、关系图 | 已完成初版；等待审查 |
| 2 | 资料管理独立页面与自动元数据 | 资料列表/详情、标题和时长提取、手工修正 | 真实录入 20 份资料 |
| 3 | 备注、多篇 Markdown 笔记与浏览时间 | 单一备注、多笔记、独立时间戳、活跃/停留时长 | 连续使用一周对账 |
| 4 | 归档/回收站/恢复 | 状态页面、影响预览、恢复 | 误删恢复演练 |
| 5 | 去重与版本关系 | URL/hash 提示、版本选择 | 重复导入用例通过 |
| 6 | 组合筛选 | 筛选器、全文搜索、最后查看时间 | 100 份模拟/真实资料找回测试 |
| 7 | 重点句和主观复习卡 | 三类卡片、来源返回 | 每类完成一次真实复习 |
| 8 | 写作/口语自评 | 清单、历史对比、行动转化 | 连续三次练习验证 |

资料关系详情和链接健康检查保留为后续待办。任务 2–6 完成后再确定学习计划的数据格式；任务 7–8 完成后再设计首页规则的最终阈值。

## 12. 计划维护规则

- 新需求先判断它属于现有阶段、需要提前补地基，还是满足某个后期触发条件。
- 不把“以后可能有用”直接升级为当前任务。
- 用户每天只需查看 `DECISIONS-NEEDED.md`；agent 可以在推荐默认值下继续不受阻的工作。
- 任何变更如果会改变现有资料含义、删除数据、产生费用或调用外部模型，必须先形成具体可审查的影响说明。
- 每完成一个阶段，用真实资料进行一次业务验收，再更新下一阶段，而不是只依据代码测试。

## 13. 当前交付计划（2026-09-16 修订版 R2）

修订依据：独立 task-feasibility 评估。R2 为问题解决方案，尚未实施；13.7 的检查点细化 13.4，所有 DEV 编号保持不变。

### 13.1 版本终点与范围

本轮规划终点：本机单用户能导入和整理资料，在站内完成练习，把重点句、写作或口语表达保存为有来源的复习卡，完成自评和复习后，从首页进入有真实依据的下一次学习；关闭并重新启动后资料和关联可找回，备份能在隔离目录恢复。

沿用现有优先顺序：资料管理 → 主观复习 → 自评 → 行动反馈。最近只细化阶段 0–1；规划不代表开始实施。保留现有四语言、听读写说、词汇、摸底、复习与周报，不要求重新开发已存在能力。

当前包含：新资料 SQLite 目录接入、基本详情与编辑、常用文件元数据、单备注/多笔记、标签与组合搜索、收藏/归档/回收站恢复、重复提示、浏览时间；随后完成手工主观卡、自评、首页与规则提示。所有新记录沿用本地用户、模块、科目归属。

明确排除：PLN-* 学习计划、IMP-QBANK-001、IMP-BOOK-001、ASM-TREND-001、AI 反馈/转写/语法、云端、多账号、第二学习模块、LIB-REL-001 完整关系面板、LIB-LINK-001 快照及链接健康。Study Package 通用交换格式、自定义保存视图、高级标签治理/推荐和 ANA-MILE-001 里程碑留待后续定义，不阻塞本轮。来源返回所需最小引用仍必须实现。自评的“下次行动”采用复习卡或进入练习的提示，不暗中实现暂缓的计划排期。

### 13.2 当前起点与实际证据

核对环境：Node v24.5.0，本机项目，无 Git 仓库可提供提交基线；证据截至本次读取。未改业务代码、未初始化或清空真实资料目录。

| 能力/环节 | 工程判断 | 证据与局限 |
| --- | --- | --- |
| SQLite 地基（STO-META-001、USR-LOCAL-001、MOD-CORE-001） | 部分实现；初始化已验证 | `scripts/init-database.mjs`、迁移 001–006；临时目录连续初始化两次，均为 6 迁移、1 用户、1 模块、6 科目、7 标签组、62 标签、6 地区；完整性 ok，外键检查无违规。未验证真实目录升级、完整地区词表和全部业务约束 |
| 文件资料保存、重启读取、音频与 ZIP | 现有接口测试已验证 | `npm run test:library` 1 项通过，覆盖隔离目录存储/重启/范围请求/ZIP 内容及 CRC；首次沙箱监听遭 EPERM，允许回环监听后通过。该测试不等于新 SQLite 目录恢复已验收 |
| SQLite 成为资料事实来源 | 未实现 | `scripts/library-server.mjs` 仍写读 `metadata.json`，列表和搜索未接目录数据库；`LOCAL-STORAGE.md` 同样明确未切换 |
| 备注、多笔记、浏览记录、生命周期 | 表结构存在，端到端尚未完成 | 迁移 001/004/006 有相应字段与表；现有 `components/study-library.tsx` 不等于独立管理流程已交付 |
| 主观复习 | 未实现 | `lib/progress.ts` 的 ReviewCard.sourceKind 只有 vocabulary/practice，需同步扩展校验、序列化、来源打开与备份恢复 |
| 复习、周报、进度 | 既有逻辑检查已验证 | `test:review` 9 项、`test:analytics` 10 项、`npm test` 16 项通过；`lint:app` 退出 0 |
| 听读写说、搜索、导入、四语言完整体验 | 部分代码证据，尚未重新体验 | 已读取相关组件与文档；本次未运行构建、practice/assessment/providers 测试或浏览器端到端；未验证真实麦克风、各音频格式和在线文章提取 |

不将能力地图的 Available 或旧计划“已完成初版”换算为整个开发任务 Done。

### 13.3 里程碑与依赖

| 阶段 ID | 用户获得的结果 | 入口/前置依赖 | 任务与状态 | 验收方式 | 出口条件 |
| --- | --- | --- | --- | --- | --- |
| 0 | 可重复建立并检查资料目录 | 当前迁移可执行 | 0.1、0.2：In progress（部分实现；仅初始化子检查已验证） | 本次隔离初始化检查；接入前补约束与旧 schema 升级回归 | 目录结构可供阶段 1 使用；不宣称已接入页面 |
| 1 | 导入、整理、找回、恢复并再次练习 | 阶段 0；先做 DEV-001 | DEV-001–007：Planned | 每任务自动检查；阶段末用混合资料走一遍整理与恢复 | 100 份样例可组合找回；重启/恢复保持文件与引用；四语言关键流程可用 |
| 2 | 重点句、作品和口语表达进入复习并返回来源 | DEV-001–004、006；卡片用到标签时接 DEV-007 | 沿用 2.1/2.2：Planned，进入时细化 | 三类各建卡、编辑、评价、重开、返回来源；缺失/归档/回收站边界 | 三类卡都能复习且历史与来源可追溯；进度备份往返无损 |
| 3 | 写作/口语完成自评并留下下一次改进点 | 阶段 2 的卡片和稳定练习引用 | 沿用 3.1/3.2：Planned | 正常完成、草稿、跳过、无录音；同类型历史比较 | 自评保存并能进入下次练习或复习，不依赖学习计划或 AI |
| 4 | 首页直接进入有依据的下一步 | 阶段 2–3；DEV-007 时间事件 | 沿用 4.1/4.2：Planned | 合成日期/时区与历史测试；新用户、忽略、完成后重算；人工入口体验 | 1 主行动、最多 2 次行动，规则最多 2 条；显示证据、可忽略、可执行；完成版本闭环 |
| 5–7 | 未来计划、题库、趋势与按需增强 | 能力地图解除 Deferred 或另行确定版本范围 | Deferred | 重新定义后独立评估 | 当前无交付承诺 |

依赖无环：DEV-001 → DEV-002 → DEV-003 → DEV-004 → DEV-006 → DEV-007；另有 DEV-002 → DEV-005，DEV-005 的版本关系完成后须回归 DEV-006/007。各任务内部依赖见 13.7；阶段 2 不必等待与卡片无关的去重或高级筛选全部完成。任务状态使用 Planned / In progress / Blocked / Ready for review / Done / Deferred。自动检查通过但必要体验尚未验证时用 Ready for review；人工集中核对阶段结果，不强制每任务审批或等待一周。

### 13.4 最近任务（沿用旧 0.x/1.x 规格，新拆分使用 DEV 编号）

所有任务开始前核对最新代码和相关依赖；以下可行性结论适用于当前基线。共同规则：只操作隔离样例做失败/恢复实验；追加迁移而不改已应用迁移；自动检查覆盖本次行为与必要回归。未完成前不得标记 Done。

**DEV-001｜新资料目录读写与文件一致性（Done；对应旧 1.1 新资料直写范围）**

- 产品关联：STO-META-001、STO-LOCAL-001、USR-LOCAL-001、MOD-CORE-001、LIB-CAT-001。
- 目标/用户问题：新保存资料需要稳定标识和可编辑目录，否则后续卡片及标签无法可靠关联。
- 范围：以粘贴 TXT/Markdown 为首个纵向入口，保存到 SQLite 和原文件目录，通过现有列表/详情重新打开；记录用户、模块、科目及稳定 resource ID；服务启动可初始化目录。其他入口在 DEV-002 接入，不提前切换全部用户入口。
- 依赖：阶段 0 已可执行的迁移，不等待 0.2 全部完成；不做旧文件回填、双写或清空。先在 DEV-001.A 固定空来源为 NULL、去除来源唯一约束、归属校验和隔离目录模式，再进入读写实现。正式切换前列明旧目录保留位置及进度引用边界。
- 规则/状态：标题非空；保存中禁止重复提交；失败保留输入；只有文件和数据库都成功才显示已保存；文件与 SQLite 无共同事务，使用暂存、事务、补偿及重启清理策略；缺文件时详情显示修复状态，不崩溃。
- 验收：连续保存两篇无来源文章和两条同来源记录→列表打开→重启→同一 ID、正文和归属仍可读；非法归属被拒绝；文件写失败、DB 提交失败及关键窗口进程退出不能出现“成功但不可读”的项目，重试不留幽灵目录项。
- 可行性：可直接开始；Node SQLite、目录模型和存储测试均可复用，难点是两种存储的一致性。AI 检查：隔离接口/失败注入/重启回归。人工：保存并重开一篇文章即可，无需额外产品决定。

**DEV-002｜全部既有保存入口与备份恢复接入（Done）**

- 产品关联：IMP-WEB-001、IMP-FILE-001、STO-LOCAL-001、STO-BACK-001、STO-PROG-001、LIS-QA-001、REA-QA-001、WRI-DRAFT-001、SPE-REC-001。
- 目标/范围：链接、文件、单词、练习存档和录音共用目录写入；练习读取与搜索使用相同 ID；保留当前格式和大小限制，浏览器进度继续使用原备份机制，暂不将全部进度迁入 SQLite。
- 依赖：DEV-001（含来源非唯一索引迁移）。规则：断网/上传失败保留表单；缺少资料引用明确显示来源不可用，不静默创建替代来源。同一材料可保存多次练习，以独立 ID 区分，不靠 URL 去重。
- 验收：文章、词汇、音频、录音和练习各保存并重开，包括同材料两次练习；备份期间受控交错写入/修改/删除仍产生可恢复的一致资料集合；含非空 SQLite、文件和进度的 ZIP 在新临时目录解包、启动，并显式导入对应进度 JSON，所有已包含来源 ID 和播放/阅读可用；失败不提供假成功下载。具体锁、清单与恢复检查见 13.7。
- 可行性：有条件可完成；既有 ZIP 测试仅验证文件内容，必须增加真实 DB 快照和恢复证据。AI：各保存入口、关联和恢复回归；人工：实际录音回听及一轮备份恢复。无麦克风时自动测试用合成音频，真实录音体验保持待验证。

**DEV-003｜独立资料管理与元数据修正（Ready for review；旧 1.2）**

- 产品关联：LIB-CAT-001、LIB-META-001、IMP-FILE-001。
- 范围/流程：管理列表→详情→修改标题、摘要、来源、类型和时长→保存→重开；TXT/Markdown 可编辑正文，PDF/音频仅元数据；保留自动值来源和提取时间，人工值不得被再次提取覆盖。
- 依赖：DEV-002。标题空/URL 无效不保存；加载、空列表、提取不全、保存失败均有状态，失败不丢输入。
- 验收：正常元数据自动填充并可修正；缺标签或损坏标签仍可保存文件并手工填写；MP3/M4A/WAV/WebM 样例逐项记录成功/降级，时长和其他修改时间不混淆。
- 可行性：常规编辑可开始于依赖完成后；多格式提取需先验证 D-013 默认范围，用每格式带/不带标签样例做最小实验，再选择库。EPUB 不纳入；PDF 更深内容提取不阻塞元数据手改。AI：字段校验、提取样例和人工优先规则；人工：混合资料编辑与重开。

**DEV-004｜单备注、多笔记与最小标签编辑（Done；旧 1.2/0.2）**

- 产品关联：LIB-NOTE-001/002、LIB-TAG-001/002/003/004/006。
- 范围：详情编辑一个 Markdown 备注，多篇笔记新建/改名/排序/编辑/删除及预览；分组标签分配/移除；17 主题默认；自定义名称/颜色校验，系统建议显示依据与可靠性，用户确认后采用。
- 依赖：DEV-003。资料、备注、笔记各有独立时间戳；离开未保存编辑提示；标签 key 不随界面语言变化；不因标签修改丢资料。
- 验收：一份资料创建 20 篇笔记，重启后内容/顺序完整；改备注不改笔记时间；标签冲突显示可修正错误；无建议依据时保持未知，不生成虚假等级。正常用例为两轮听写笔记；边界为同名系统标签及未保存离开。
- 可行性：有条件可完成，schema 可复用，需核对名称/颜色规范和 Markdown 安全渲染；D-006 可按默认值继续。AI：持久化、冲突、时间戳、预览注入检查；人工：四语言标签与备注/笔记区分。

**DEV-005｜重复提示与保留版本（Planned；旧 1.4）**

- 产品关联：LIB-DUPE-001。
- 范围：URL 规范化、文件 SHA-256、正文哈希提示；打开已有、取消、保留独立版本并追踪关系；标题相似不能自动去重。
- 依赖：DEV-002；来源非唯一索引已由 DEV-001.A 提供，本任务只增加规范化/哈希提示与版本关系。以重复 URL fixture 回归，不在这里首次修订该约束。
- 验收：同文件重传出现已有项；同 URL 更新正文可保留两版；取消不留临时文件；重复提交不静默合并。
- 可行性：有条件可完成；前置约束由 DEV-001.A 修订，不需产品审批。AI：URL/hash/前置迁移回归/取消清理；人工：一次保留新版流程。

**DEV-006｜收藏、归档、回收站与恢复（Planned；旧 1.3）**

- 产品关联：LIB-LIFE-001/002、REV-HIST-001。
- 范围/流程：详情收藏→归档→含归档搜索→恢复；移回收站→查看影响→恢复，保留标签和现有练习关系。回收站不自动清空；彻底删除仅在详情影响预览后操作。
- 依赖：DEV-004；若已有版本关系则一并覆盖 DEV-005。归档不改历史；回收站资料不推荐；来源不可用仍保留学习历史。永久删除前明确保留的历史快照、移除的文件与失效引用表示方式，若与已确认规则冲突则记录产品决定再做该分支。
- 验收：带练习与复习引用资料误删后完整恢复；取消删除无变化；永久删除后没有声称可播放的悬空引用。UI 覆盖空回收站、归档、失败与恢复状态。
- 可行性：软删除可行；永久删除有条件，浏览器进度与数据库分离，影响预览需覆盖两端。AI：状态转换、引用和取消/失败回归；人工：一次误删恢复演练。

**DEV-007｜组合找回与可信浏览时间（Planned；旧 1.5）**

- 产品关联：LIB-TAG-005、SRCH-FACET-001、SRCH-CMD-001、SRCH-AUDIO-001、LIB-TIME-001、ANA-EVT-001。
- 范围：管理页及统一搜索共用标签/类型/技能/日期/收藏/状态语义；标题、正文、摘要、备注、笔记检索；同组 OR、跨组 AND，返回保留条件，空结果可逐项清除；无转写音频只匹配元数据。
- 依赖：DEV-004/006；版本过滤回归使用 DEV-005。浏览时长区分停留/活跃/最后查看；默认 60 秒无交互停止活跃计时，隐藏页面不累计，异常关闭按最后心跳结算，实际听音继续单列。
- 验收：100 份合成混合资料通过不超过三次筛选找到目标；笔记全文可找回；前台活动 3 分钟、后台 20 分钟、再活动 2 分钟只累计约 5 分钟活跃量；重开不重复记账。
- 可行性：有条件可完成；需验证现有 FTS 对中日文查询的实际效果和事件去重，必要时小数据集采用有界文本匹配补充。AI：筛选真值表、查询、可控时钟与重启；人工：实际浏览器前后台及四语言搜索。保存筛选视图另行安排。

### 13.5 关键风险、决定与替代范围

| 问题与证据 | 影响 | 处理/所需证据 | 当前是否阻塞 |
| --- | --- | --- | --- |
| 文件与 SQLite 无共同事务；旧接口仍扫描目录 | DEV-001/002 的保存完整性 | 暂存/补偿/启动恢复，失败注入证明不暴露半成品；先 TXT/MD 纵向验证再扩入口 | 不阻塞 DEV-001 开始 |
| source_url 唯一索引禁止无来源空串重复及同 URL 多条 | DEV-001/002/005 | DEV-001.A 将空来源转 NULL、追加迁移换为非唯一索引，以 ID 区分资料/练习；DEV-005 仅做提示和版本关系 | 不阻塞 DEV-001.A 开始；DEV-002 必须在该迁移后 |
| 浏览器进度与资料数据库分开；备份只限制并行备份 | DEV-002/003/006、阶段 2 | DEV-002.B 加资料写入互斥、备份清单和显式进度恢复；后续编辑/删除必须复用互斥并回归 | 不阻塞起步，阻塞未经一致性/恢复测试的交付 |
| D-013 格式提取尚无样例运行证据 | DEV-003 | 四音频格式最小样例；失败可人工修正，不能宣称自动提取全覆盖 | 默认方案不阻塞其他任务 |
| D-006/D-014 尚待确认但标为非阻塞 | DEV-004/007 | 采用 17 主题/60 秒；技术库选择不再升级为审批 | 否 |
| 主观卡、自评和规则在地图中仍 Defining | 阶段 2–4 | 进入阶段时把旧 2.x–4.x 用例补成任务规格；特别明确卡片长度/重复判定、自评清单、无足够历史时规则行为 | 不阻塞资料管理；有行为歧义再记录产品决定 |
| 地区目录只有 6 个种子，0.2 未完整验收 | 阶段 0、DEV-004/007 | DEV-004.C 负责离线完整地区数据、稳定代码与版本；DEV-007.A 验收选择/筛选；0.2 保持 In progress | 不阻塞最小导入流程，阻塞地区完整交付 |
| 真实麦克风、在线来源、浏览器恢复尚未体验 | DEV-002、版本验收 | 自动检查用样例，真实体验结果单独记录；网络失败保留文件/粘贴入口 | 不阻塞本机开发 |

D-015 链接健康、D-016 等级数据许可属于后续范围，不在本轮提前调研或申请服务。没有工时或权重依据，不给完成百分比或虚构工期。

### 13.6 下一任务与交接

下一项：**DEV-001，新资料目录读写与文件一致性**。现在可开始的依据是隔离 SQLite 初始化/完整性和旧存储接口测试通过，现有 C-011 已排除旧文件回填/双写，目标与验收不依赖外部服务或待确认产品决定。实施前读取最新迁移和接口，核实目录配置，先完成 13.7 的 DEV-001.A 来源/归属/目录规则，再按 DEV-001.B 完成 TXT/Markdown 保存、列表读取、重启及失败补偿；两项验收通过后再接 DEV-002。

交付记录必须回填实际范围、差异、运行命令、结果、待体验项和下一依赖。当前所有 DEV 任务均为 Planned；本次只更新规划，不实施任何开发任务。

### 13.7 独立评估问题的解决方案（R2）

本节明确实施默认方案、检查点和关闭问题所需证据。以下均为 Planned，不是代码修复记录。涉及技术选择可在不改变业务语义的前提下用更简单、经验证的等效方案替换，并记录理由。

#### A. 将来源约束、归属和目录切换前移到 DEV-001

**DEV-001.A：目录协议与增量迁移。**

- 空白/缺省来源统一为数据库 NULL；非空来源沿用已有 URL 校验，原始有效来源不因去重丢失。追加迁移将活动 URL 唯一索引替换为查询用非唯一索引；不修改 001–006。稳定资源 ID 是身份，URL 仅是来源与重复提示信号。
- 每次新建记录写入 `user-local-default`；从入口明确选择的技能映射至有效 IELTS subject，再由 subject 推导 module。不能判断科目时让当前入口提供选择，不伪造阅读科目；非法/停用 subject、错误模块映射拒绝写入，保留输入。同一次事务完成资源与归属关系。
- 新目录模式只读 SQLite 目录，文件只存内容和附件，不写旧格式元数据用于第二套列表。旧文件模式保持现状；模式按目录显式标记，禁止一个目录混合两套事实来源。DEV-001 只在隔离目录启用新模式及 TXT/MD 入口；DEV-002 完成前不将未接入入口开放到新模式，也不切换用户默认目录。
- 配置必须核对 `IELTS_LIBRARY_DIR` 与 `IELTS_DB_PATH` 的组合，开发检查显式指定二者；避免 DB 环境变量把隔离操作指向真实目录。新模式拒绝不符合目录约定的数据库路径，或在实现时给出同等明确的定位/备份规则；不得静默读另一目录的数据。
- 自动验收：从空库和已执行 001–006 的合成 SQLite fixture 升级，原记录 ID/归属不变；两篇无来源、两条同 URL、新资料关联同一来源均成功；无效归属拒绝且无半条记录；明确模式下不从旧目录偷偷补读。

**DEV-001.B：首条读写流程与崩溃恢复。**

- 先写暂存文件并验证可读，再完成目录记录与文件就绪的提交协议；实现者选一个可恢复的持久化操作状态/日志，启动时完成或清理未完成动作。仅请求异常补偿不够，必须覆盖进程被终止后不能执行 finally 的情况。
- 验收覆盖文件写入前、文件就绪后但 DB 未提交、DB 提交附近、成功响应前终止。重启后每个请求对应完整记录或明确未完成可重试状态；不会出现可见但文件不可读的“已保存”资料。提交后响应丢失的重试使用稳定操作 ID 防止无意重复保存；用户明确新建另一份仍获新 ID。
- 回退：独立保存改动和迁移记录，开发中可退回旧模式与原目录；新目录数据先保留，不能用删除目录作为业务回滚。数据库采用前向修正，不对真实记录运行破坏性 down migration。没有 Git 时先保存受影响文件副本和可核对差异。

#### B. 将 DEV-002 拆成保存入口和一致备份两个检查点

**DEV-002.A：入口与引用。** 链接、文件、词汇、录音和练习档案逐类接入统一资源写入服务，各自有成功/失败/重启样例，最后检查同材料两次练习与原文+练习共存。修改展示类型不会改变 resource ID 或物理文件路径；旧 `kind/id` 入口需通过稳定 ID 解析或兼容跳转，DEV-003 更改类型后仍可打开。该检查点完成后可以审查入口，但整个 DEV-002 在备份恢复完成前不能 Done 或默认切换。

**DEV-002.A 引用协议补充（复审修订）：** 现有 `ActivityEvent.resourceId` 可能是 URL、词语或内置材料标识，不得直接当数据库外键。新写入在不破坏旧字段含义的前提下增加可判别的引用描述，至少区分本地资产、内置材料、外部来源和词汇活动键；只有明确本地资产引用使用目录 resource ID。旧 JSON 导入根据记录类型和已知映射兼容，不按“像 UUID”或“像 URL”的外观推断所有权；无法确定的旧引用保留为 legacy，显示解析状态，不删除记录、不伪造目录项。格式演进需同步 progress 校验/序列化和备份往返测试。

备份验收增加四种引用各一条与一条无法解析的旧记录：非本地引用和 legacy 均能无损导出/导入，清单区分资料文件完整性与引用解析状态。不能把外链/内置来源不存在于 SQLite 当成备份失败，也不能把 legacy 显示成已验证可返回的本地资料。

**DEV-002.B：备份一致性与恢复。**

- 本机单服务模式采用统一资料写入互斥：所有保存、编辑、标签、笔记、归档、删除、迁移等修改必须进入同一写入协调器。备份先等待已接收写入完成，再持有独占锁，捕获 DB 快照与所引用文件、校验并生成 ZIP，成功或失败都释放锁。备份期间新的修改返回可重试“正在备份”，保留表单，不显示保存成功；读取继续可用。
- 每个资料目录只允许一个写服务拥有写入权；第二服务启动失败并说明目录被占用。锁需有异常退出恢复策略，不能只用永久残留标记文件。备份脚本只能通过协调器执行；独立离线备份须在服务停止后使用。外部手动修改源文件不属于应用写入事务，但缺文件/校验失败必须使备份失败，不能输出完整性成功。
- 备份枚举来自 SQLite 中的文件关系，不再依赖即将停止双写的 `metadata.json` 扫描。清单保存格式/schema 版本、资料/文件相对路径及大小/校验值、所附进度快照文件名及捕获时间。数据库副本用现有 backup API，禁止直接复制活跃 WAL 数据库文件。
- 进度仍是当前浏览器在点击备份时捕获的已保存 JSON，不承诺所有标签页/浏览器的同一瞬间全局快照。后续浏览器变化不进入此次备份。锁内仅对明确本地资产引用做强校验：有效本地来源必须在资料快照中，已明确删除的本地来源以墓碑/不可用状态记录，其他缺失本地资产引用应明确报错或列入不完整清单并禁止标为资料完整备份。内置、外部、词汇活动键和无法确定的 legacy 按 DEV-002.A 分类原样保留，单列可解析性，不能套用本地外键规则。未保存草稿继续不在备份范围。
- 恢复分三步：解包到隔离目录并校验清单/DB 完整性/文件校验值；启动该目录；在空测试浏览器配置中显式导入清单指定的进度 JSON。验证卡片、历史、来源返回和播放，不能仅解包就声称浏览器进度已恢复。
- 自动验收：受控交错“保存在锁前/锁中”“修改在锁中”“删除在锁中”；每份成功备份内部引用与文件一致。故意缺文件/改坏内容、备份脚本失败、服务异常退出均不能返回成功 ZIP，之后可重新备份和写入；第二服务不得绕过锁。DEV-003/004/006 增加写接口时必须接协调器并重跑相关交错场景。
- 回退：先关闭新备份入口、保留历史 ZIP；修正后重试，不改写已发布备份。DEV-002.A 的已保存内容保持可读。成功 ZIP 使用临时文件完成校验后再发布。

#### C. 将 DEV-004 分开验收，补齐地区目录和颜色模型

| 检查点 | 具体结果与关闭标准 | 前置依赖/回退边界 |
| --- | --- | --- |
| DEV-004.A 备注与笔记 | 单备注、20 篇笔记、排序/预览/时间戳；失败保留编辑，内容与备份可往返 | DEV-003；隐藏未通过的新入口而保留已写内容，不删除笔记数据 |
| DEV-004.B 手工及自定义标签 | 分组稳定 key；新增自定义颜色持久化字段或等效结构，系统组色不变；按规范校验四语言系统名称和规范化颜色冲突；停用系统标签不能新分配，历史关联保留 | DEV-003；迁移追加；回退 UI 不回收已分配标签 |
| DEV-004.C 完整地区基础数据 | 按 TAG-TAXONOMY 的世界/区域/国家层级及稳定代码载入完整离线目录；数据来源、版本、校验值、更新策略有记录；四语言名称缺译时明确回退，不伪称完整翻译；选择区域不自动写入下辖每国关系 | DEV-003，可与 A/B 独立完成；先验证公开官方来源与使用条件，不在此宣称已取得数据；保留旧 ID，新增代码映射/停用而不删除历史关系 |
| DEV-004.D 可解释标签建议 | 采用已有本地规则或导入元数据生成候选，持久化规则/字段依据及可靠性；用户确认后应用；无依据保持未知，不接 AI、不伪称官方等级 | DEV-004.B，建议涉及地区时还依赖 C；可关闭建议入口而保留手工功能 |

DEV-004.C 的实施前验证必须确认数据获取和再分发条件，检查代码唯一、父子无环、M49/国家码映射及与所选版本源清单逐条对账；不固定未经核实的国家总数，不使用 6 个样例冒充完整。联网暂不可用时可继续 A/B/D 的不相关部分，但 C 不得 Done，也不能宣称地区范围已交付。按 C-006 只开放 coverage，不恢复来源地 origin 的界面。

0.2 的剩余验收分别由 B（稳定 key、组色、四语言显示与停用历史）、C（地区）、D（建议依据）提供证据后回填。0.1 还需 DEV-001.A 的升级 fixture 和 DEV-002 的启动读取回归；初始化子检查已验证不等于两项历史任务整体 Ready for review。

#### D. DEV-006 分开软删除与永久删除

- **DEV-006.A：** 收藏、归档、回收站、恢复与现有引用状态；依赖 DEV-004。恢复保留原 ID、标签、笔记和练习关系；归档不改学习历史。软删除恢复先独立通过，不因永久删除尚未完成而无法检查。
- **DEV-006.B：** 永久删除的影响预览、确认、文件清理和失效引用；依赖 A 与 DEV-002.B 的协调器。默认方案是保留独立学习事件、已保存回答/卡片内容及来源 ID，文件被删除后显示“来源已永久删除”，不提供虚假播放/返回；资料本体及其备注/笔记属于删除范围，预览应明确列出。取消不改变任何一端。
- 浏览器 JSON 不能由 SQLite 事务原子更新：服务端保留最小删除墓碑，客户端重开/导入旧进度时按墓碑识别来源不可用，不静默删掉学习历史。预览分别列出目录中可确定关联与当前浏览器进度关联；不能声称已扫描其他浏览器。若用户范围要求恢复被永久删除文件，只能从删除前备份恢复，不将永久删除伪装为可直接撤销。
- 上述保留范围是供实施使用的方案；DEV-006.B 开始前须对照产品决定核对是否改变已确认含义，仍有冲突才追加产品决定。不阻塞 A 或 DEV-001。AI 验收包括取消、删除中断恢复、引用墓碑、旧进度导入、删除前/后备份各自恢复；人工体验一次软删除恢复和一次仅用合成资料的删除影响预览。
- 版本关系由 DEV-005 引入后，DEV-005 收尾必须证明删除其中一版不删除另一版且历史引用正确；若 006/007 已完成，要回归其相关用例，不能把后置分支造成的回归默认为别人已验收。

#### E. DEV-007 拆为检索和计时，确定中文检索补充路径

**DEV-007.A 搜索/筛选：** 依赖 DEV-004.A/B/C 及 DEV-006.A；主任务关闭前同时覆盖 DEV-004.D/006.B 对结果状态的影响。英文字词可沿用 FTS；中日文采用参数化、字面子串匹配补充标题、摘要、正文、备注与笔记，先应用相同的状态/标签/地区条件，再合并去重结果。查询中的 `%`、`_` 等必须按字面处理，不能改变用户语义或拼接 SQL。规范化查询仅影响搜索键，不重写正文和显示名称。

针对当前 100 份资料规模，默认在数据库内匹配分页返回，不能先截断前若干条再搜索并假装没有更多结果。定义取消/超时可见状态，不把超时当零结果。验收固定样例包括中文“国际金融新闻”查“金融”、日文连续文本片段、简繁各自原文、英文词语、混合字符、特殊符号、备注/笔记命中、跨页目标及地区组合；不额外承诺简繁自动互转或日文词形还原。记录 100 份代表性大小材料的查询耗时与浏览器响应，有明显阻塞时优化索引或异步查询后再交付。地区选择与筛选必须包含种子之外的国家、区域及多地区资料；查询区域仅匹配已声明的关联语义，不自动扩写资料标签。

**DEV-007.B 访问计时：** 依赖 DEV-003 的详情稳定 ID 与 DEV-002 写协调器，不依赖搜索算法；最终阶段 1 收尾时与 A 集成。可控时钟验证页面可见性、60 秒无交互、最后活动、心跳重试/去重、异常关闭与备份期间写入受阻后重试。实际音频播放与录音秒数沿用独立计数器，不能与阅读活跃秒数重复汇总为同一种活动。人工只需一轮前后台切换和回听，不强制等待一周。

回退：搜索与计时分别保留可独立关闭的入口/逻辑；已保存访问记录保留，算法修正时不默默重写历史。主 DEV-007 的状态由 A/B 两项及集成回归共同决定。

#### F. 修订后的开始条件和复审要求

- 下一检查点是 DEV-001.A，可直接开始实施；DEV-001.B 依赖 A，DEV-002.A/B 依赖 DEV-001 完成；DEV-003 依赖 DEV-002 全部通过。之后 `003→004(A/B/C/D)→006(A/B)→007` 为阶段关闭链，`002→005` 为版本提示分支；13.7 允许不依赖后项的内部检查点先做，但不会把父任务提前标 Done。
- 每个检查点继承父任务产品 ID 和验收规则，实施记录单列实际命令、结果和未验证项，不重排 DEV 编号，不增加逐项人工审批。
- 请独立评估 agent 逐项判断原问题是否已在计划层解决，检查新增方案是否引入范围冲突、循环依赖或不可兑现保证；未实施的写入锁、恢复协议、地区数据和新搜索只能列为待实施验证，不能因方案更详细就声称测试通过。

### 13.8 DEV-001 实施记录（2026-09-19，进行中）

- 用户授权调度开发实现 agent 完成第一个任务 DEV-001（A+B），问题先由调度者处理，仅无可靠解决方案或影响产品取舍时交用户。
- 实施约束：优先复用现有模块；最小完整变更；不提前做通用框架，不增加无依据的防御分支。保留输入校验、文件与数据库一致性、重试及崩溃恢复等直接对应本任务验收的必要处理。
- 恢复检查：此前中断于读取阶段，尚无 DEV-001 业务代码变更。当前由原开发实现 agent 继续，状态 In progress；DEV-002 及之后仍 Planned。
- 方案选择：复用现有初始化、资料路由、UI 与测试组织，只为 SQLite 目录增加必要实现。尚未运行本次实现验收，不将原评估结果当作开发完成。

### 13.9 DEV-001 交付记录（2026-09-19）

状态：Ready for review。A+B 工程实现及自动验证完成，浏览器实际点击体验未验证；未开始 DEV-002。

完成：追加 007 来源非唯一索引及保存操作记录；复用初始化函数、现有资料接口和UI，在隔离 SQLite 模式中支持 TXT/Markdown 粘贴保存、列表、详情、原文下载和重启读取；空来源为 NULL，校验有效用户/科目/模块；操作 ID 实现重复请求幂等；pending/committed 区分提交前后，异常补偿及进程中断恢复；缺文件保留目录并显示修复状态。未接入入口拒绝写入，默认旧目录不变。

实现方式：新增一个 SQLite 存储模块和一张操作状态表，沿用现有接口/组件，未增加依赖、通用框架或后续阶段架构。单本机服务使用，不承诺多进程或整机断电事务性；所有权锁和一致备份仍属 DEV-002。

实际验证：开发 agent 已报告旧模式 `test:library` 1项、`lint:app`、`build:cloudstudio` 通过；调度者在补齐最后故障用例后实际运行 `test:library:sqlite`，6项全部通过，并重新运行 `lint:app` 通过。新测试覆盖空/重复来源、归属拒绝、幂等、重启、缺文件、4个异常窗口和4个SIGKILL窗口、001–006合成数据库升级、目录模式隔离、真实文件写入失败及数据库触发器拒绝。所有写入测试使用临时目录。隔离预览根页、status、items均HTTP 200；此结果不等于浏览器点击验收。

独立只读审查：在最终补充测试前审查保存/恢复/重试及UI操作ID路径，未发现阻塞缺陷或明显过度设计。两位子agent随后因额度中断，调度者接手核对新增测试并完成运行，不冒称有最终第二轮独立审查。

体验入口：http://localhost:3013/ 。进入电脑资料库，输入标题、选择科目、粘贴正文，保存后从列表重开；刷新后资料仍可读。新目录 `/Users/ouki/Documents/Codex/ielts-dev001-preview` 与原资料隔离，当前尚未提供新模式ZIP备份。服务重启持久性已由自动测试覆盖。

回退：停止隔离预览，使用原命令启动旧目录；保留试用目录，不删除数据。编辑前源文件副本在本任务 `work/dev001-before`，启动与限制说明已写入 `LOCAL-STORAGE.md`。下一任务 DEV-002 保持 Planned，不自动执行。

### 13.10 DEV-002 启动记录（2026-09-19）

用户授权按原调度流程继续下一任务，本次明确为 DEV-002 A+B，不是里程碑阶段2主观复习。开发执行 agent 已恢复；复用 DEV-001 现有实现及通过的工程测试，DEV-001 的人工体验仍保留 Ready for review，不阻止不依赖该确认的接口和备份实施。

范围：其余既有保存入口接入、类型明确的来源引用、单目录单写者与备份协调、目录/文件/当前浏览器进度备份及隔离恢复。保留原默认目录和进度格式兼容；不提前开发DEV-003编辑或DEV-006删除界面。技术问题先由调度者处理；优先复用、不做过度设计或无依据防御。

状态 In progress，尚无本次完成结论。

### 13.11 DEV-002 交付记录（2026-09-19）

状态：Ready for review。A+B 工程实现、集成测试和只读审查完成；真实麦克风、真实在线来源与浏览器逐项体验未验证。DEV-003仍Planned，未自动开始。

完成范围：复用现有persist和SQLite保存日志，接入粘贴、上传、词汇、公开链接、音频、录音与练习档案；文件原始字节参与重试身份，练习附件采用稳定子操作ID；所有入口复用现有界面。新增resourceRef区分本地资产、内置、外部、词汇键和legacy，保留原统计标识及旧进度兼容。

备份与恢复：本机OS文件锁先于数据库初始化/未提交清理取得，备份子进程继承锁；备份等待已接收写入，拒绝之后写入并保留表单。ZIP枚举SQLite文件关系，校验文件、目录模式、数据库与进度清单。恢复脚本只写尚不存在的新目录，校验ZIP路径/清单/文件摘要/数据库；进度仍需在目标与备份中显式导入。新增008迁移，001–007未改；未提前实现资料编辑或删除界面。

实际验证：调度者最终运行带类型剥离参数的library-integration、library、sqlite-library三个测试文件，12项全通过（DEV002集成5项、旧模式1项、DEV001回归6项）。覆盖混合资料保存/稳定附件重试、备份恢复后进度parse等值与卡片来源/音频范围读取、锁交错、缺失/损坏资料拒绝、子进程失败重试，以及父服务SIGKILL后备份子进程仍持锁/最后FD关闭可重开。调度者另外运行lint:app、tsc --noEmit、progress迁移/往返、review 9项、analytics 10项和Python语法检查通过；build:cloudstudio由开发agent运行并报告通过。测试均为隔离合成数据，可控网络响应不等于真实网络来源验收。

独立审查：原评估agent已只读审查锁/备份/恢复及4项核心集成测试断言，未发现必须修复问题或明显过度设计；第5项父子持锁由最终实际测试补证。开发agent因额度中断后，调度者完成最终验收与交付，不把中断本身当功能失败。

预览：http://localhost:3013/ 已重启加载本次代码，隔离目录仅追加008迁移；根页面、status、items均HTTP200。原默认资料目录保持不变。当前预览列表为空，供用户体验。实际浏览器点击/录音授权尚未执行，不宣称用户已验收。

最短体验：刷新预览，在资料库保存文本/单词或上传音频并重开；在练习中心保存一次练习；打包备份，按LOCAL-STORAGE.md恢复到新目录，再在目标与备份中导入清单指定的进度JSON。必要人工项为录音回听及恢复后的来源返回。不得仅解包就视为浏览器进度恢复。

实现约束与回退：未新增第三方依赖、通用框架或云部署。复用Python fcntl，本轮验证macOS/Node24.5，Windows未承诺。npm启动/相关测试加显式类型剥离参数以复用同一进度校验；不复制校验代码。回退为停止新服务并保留新目录和ZIP，使用原legacy目录；本次修改前副本与精确差异在work/dev002-before。已运行升级的目录不执行破坏性降级。

### 13.12 DEV-001 / DEV-002 人工验收记录（2026-09-19）

用户确认当前列出的资料保存、打开、练习中心使用及其他必要体验均通过，因此 DEV-001 与 DEV-002 更新为 Done。资料库文章可以从练习中心打开。

用户观察到当前类别展示与预期略有差异，但明确表示不影响当前使用。该项不回退 DEV-001/002，也不新增阻塞决定；归入 DEV-003 的资料类型、元数据展示与人工修正流程中一并核对。DEV-003 仍为下一项 Planned 任务。

### 13.13 DEV-003 启动记录（2026-09-19）

用户授权按原调度流程开始 DEV-003，并同时要求整理次日云端部署准备资料。DEV-003 状态更新为 In progress；云端工作本轮只整理准备清单，不注册、部署或改变本机优先范围。

DEV-003 继续遵循最小实现约束：复用现有 SQLite 目录、资料接口、写入协调器和界面；只实现资料管理、元数据提取及人工修正，不提前纳入 DEV-004 标签笔记、DEV-005 去重、DEV-006 生命周期或云端存储改造。测试使用隔离目录，不修改真实资料。

### 13.14 DEV-003 交付记录（2026-09-20）

状态：Ready for review。工程实现、隔离自动测试、静态检查、完整构建和 HTTP 健康检查已完成；浏览器实际点击体验仍需用户核对，未自动开始 DEV-004。

完成范围：复用现有资料列表与详情，增加标题、摘要、来源、逻辑类型、科目和时长编辑；TXT/Markdown 正文可编辑，PDF、音频和练习 JSON 只允许元数据编辑。界面显示标题来源、提取时间、时长来源/更新时间和资料更新时间；保存失败保留表单，未保存离开提示，存在未保存手工修改时禁用重新提取。逻辑类型修改不移动物理文件，也不改变稳定 resource ID。

提取与一致性：使用本机 ffprobe 读取 MP3、M4A、WAV、WebM 时长和内嵌标题，不新增 npm 依赖。无标签或损坏文件允许降级并保留人工编辑；title_source 或 duration_source 为 manual 时重新提取不覆盖。正文编辑写入新版本文件，在事务中切换文件引用、正文和摘要；009 迁移记录小型编辑操作，启动时清理未提交新文件或已提交旧文件。所有编辑和重新提取继续使用 DEV-002 写协调器，备份期间拒绝修改并保留可重试状态。

实际验证：调度者运行 `test:library:edit` 5 项、`test:library:integration` 5 项、`test:library:sqlite` 6 项和旧 `test:library` 1 项，合计 17 项全部通过。覆盖完整编辑与重启/备份、四格式带/不带标签 8 样例、人工值优先、4 个异常窗口、4 个 SIGKILL 恢复窗口，以及编辑/重提取与备份互斥。`lint:app`、`tsc --noEmit`、`build:cloudstudio` 和完整 `build` 均通过。首次受限沙箱运行因禁止监听 127.0.0.1 而全部 EPERM；获准使用本机回环地址后同一组功能测试全部通过，该环境失败不计作产品缺陷。

预览：`http://localhost:3013/` 已加载 009 迁移，并继续使用 `/Users/ouki/Documents/Codex/ielts-dev001-preview` 隔离目录。根页、status 与 items 接口均 HTTP 200；真实资料目录未修改。最短人工验收为：打开一篇 TXT/Markdown 修改元数据和正文并重开；打开音频执行重新提取后手工修改标题/时长，再次提取确认人工值保留；修改逻辑类别后确认原资料仍可打开和播放；尝试离开未保存表单确认提示。

范围与回退：未加入标签、笔记、去重、删除、云端存储或新通用框架。开发 agent 因额度中断，未生成承诺的 `work/dev003-before` 副本，因此不宣称存在本轮精确前后补丁；DEV-002 的源文件副本仍可作为回退基线。回退时停止当前预览、保留隔离资料和数据库，恢复 DEV-002 代码并以迁移前备份启动新目录；不对已升级目录执行破坏性降级。

### 13.15 DEV-004 启动记录（2026-09-20）

用户授权继续 DEV-004；DEV-003 保持 Ready for review，稍后由用户检查，不因后续开发提前标 Done。DEV-004 状态更新为 In progress。

实施继续采用调度流程：独立可行性 agent 复核 A 备注/笔记、B 手工与自定义标签、C 完整地区基础数据、D 可解释建议；地区研究 agent 单独核对权威来源、版本、代码体系及再分发条件；开发执行 agent 等关键结论后按最小顺序实施。技术问题先由调度者解决，只有缺少可靠方案或发生产品取舍时再询问用户。

范围约束：复用现有 SQLite schema、资料详情、写协调器、备份语义和标签基础数据；不提前开发搜索、生命周期、AI、云端或来源地 origin；不增加通用框架、无依据防御或不必要依赖。地区数据不能以现有 6 个种子冒充完整，必须记录来源、版本、校验值、父子关系、代码映射、更新策略和多语言回退。所有写入测试使用临时目录，不修改真实资料；实施前建立 `work/dev004-before` 副本与清单。

### 13.16 DEV-004 交付记录（2026-09-20）

状态：Ready for review。A/B/C/D 工程实现、隔离自动测试、静态检查、完整构建、HTTP 健康检查及只读浏览器界面核对完成；用户实际保存体验尚待检查，未自动开始 DEV-005/006/007。DEV-003 仍保持 Ready for review，等待用户稍后一起检查。

完成范围：资料详情支持一个 Markdown 备注和最多 20 篇独立笔记，笔记可新建、改名、排序、编辑、删除并以安全文本块预览；备注、笔记和资料元数据分别维护修改时间。支持受控标签分配/移除、自定义标签名称与 Sakura/Navy/Olive/Charcoal 颜色校验、系统标签冲突提示和停用标签历史保留。第一版只开放 coverage 地区多选，不开放 origin，选择区域不展开写入所有下辖国家。确定性建议只依据现有资料类型、科目和可解释关键词，显示依据与可靠性；用户确认并保存前不创建标签关系，无可靠依据时保持未知，不推断地区、CEFR 或考试等级。

地区数据：应用内置 Unicode CLDR 48.2 派生快照与 Unicode License v3，最终 JSON SHA-256 为 `746119f7df052ac173a27676e6e0a29f602019582112a08899e308d52c8611a9`。010 迁移增加 M49/ISO alpha-2/alpha-3 映射、自定义标签颜色、建议记录和 place dataset manifest；本次快照观察到 278 个节点，但不把数量设为永久产品常量。初始化校验 hash、代码唯一、父级存在、无环、映射和四语言名称，并保留 place-world/africa/americas/asia/europe/oceania 六个旧 ID。UN M49 在线视图只用于对账，不随产品再分发其原始数据。

实现边界：复用一个资源级 organization PATCH、一个 suggestions POST、现有 revision、统一写协调器、详情组件和备份机制；没有新增 npm 依赖、通用标签框架或额外路由体系。Markdown 不使用 HTML 注入。所有组织写入在备份占用协调器时返回可重试冲突并保留表单。

实际验证：调度者独立运行 DEV-004 organization 4 项、DEV-003 edit 5 项、DEV-002 integration 5 项、DEV-001 sqlite 6 项和 legacy 1 项，共 21 项全部通过。覆盖 20 笔记排序/重启/时间戳隔离/备份恢复、Markdown 注入原文安全、自定义名称与颜色冲突、停用历史、建议确认前无关联、地区 hash/映射/父级/无环/旧 ID、备份锁 409，以及既有保存/编辑/恢复回归。`lint:app`、`tsc --noEmit`、`build:cloudstudio` 和完整 `build` 均通过。旧测试中固定 schema 009/迁移数 9 的断言更新为 010/10 后通过，不是产品缺陷。

预览：`http://localhost:3013/` 已加载 010 迁移并继续使用 `/Users/ouki/Documents/Codex/ielts-dev001-preview` 隔离目录；根页、status、items 均 HTTP 200。只读浏览器核对已确认备注、笔记、受控/自定义标签、完整地区列表、建议和保存入口实际出现，未替用户保存或修改任何样本。

回退：本轮开始前副本与清单位于 `work/dev004-before`，最终变更清单位于 `work/DEV-004-CHANGED-FILES.txt`。回退时停止当前预览、保留隔离数据库与备份，恢复开始前代码并以迁移前备份启动新目录；不对已升级目录执行破坏性降级。

### 13.17 本地资料迁移决定（2026-09-20）

用户确认当前本地资料、隔离预览资料及其学习进度仅用于开发和体验，不需要上传或迁移到未来 Cloudflare D1/R2；云端持久层正式启用时从空数据开始。后续可以丢弃这些本地样本，但删除仍须在用户明确要求时执行；在此之前继续保留，以支持 DEV-003/004 人工验收和本机回归。任何云端导入脚本不得默认扫描或上传 `/Users/ouki/Documents/Codex/ielts-dev001-preview`、默认本地资料目录、SQLite、备份 ZIP 或浏览器进度。

### 13.18 DEV-004 人工验收与已知问题（2026-09-20）

用户完成本机隔离预览验收：备注保存与重开、笔记顺序调整、备注/笔记时间戳隔离、系统主题等标签保存、地区选择、当前建议功能、未保存离开提示和语言切换均确认可用。标签建议未做深度准确性检查，用户接受当前可用状态。

已知问题：详情页无法完成自定义标签创建；因此同名系统标签冲突场景也无法从界面验证。系统主题等既有标签仍可正常保存。用户明确决定本阶段不修复，将问题记录为后续迭代项；该问题不回退已通过的 DEV-004 其他能力，DEV-004 按本次人工验收更新为 Done。

后续迭代约定：每个开发目标优先建立 GitHub Issue，Issue 记录范围、验收和已知限制；实现提交关联 Issue；阶段完成后创建并推送 Git tag，使仓库可从 Issue、提交与 Tag 追溯本次修改。

### 13.19 云端数据与文件阶段启动（2026-09-20）

用户决定暂停 DEV-005，将 Cloudflare 数据和文件云端化作为下一阶段：结构化数据进入 D1，文章、PDF、音频和录音进入 R2，部署后的工作台不再依赖本机服务在线。GitHub Actions 继续负责自动部署，优先在免费额度内运行。当前本地、隔离预览资料和浏览器进度不迁移，云端从空数据开始。

本阶段先由开发规划、任务可行性和云端架构三个 agent 独立评估，再由调度者合并方案；结论可实施时直接交给开发执行 agent，只有身份、安全、平台限制或工具权限缺少可靠方案时才向用户追加问题。实现优先复用现有 `/api/library` 产品语义、schema、详情界面、备份验收和 GitHub Actions，不做 UI 重设计，不提前恢复 DEV-005 或后续生命周期/搜索任务，也不建立无当前需求的通用云平台层。

当前进度：CLOUD-001/002 的本地基础实现与审查修复进行中。D1/R2 文本保存、列表、详情和编辑已具备本地验证路径；生产资源、Access 和远端迁移仍未执行。CLOUD-002 所要求的 organization（备注、笔记、标签、地区）尚未实现，因此 CLOUD-002 保持 In progress，不视为完成。
