# 本地资料保存

资料目录默认是 `/Users/ouki/Documents/Codex/ielts-study-library`，与网站源码分开。这个目录不会进入网站静态发布包。

在工作台左侧打开「我的资料库」：

- **文章**：粘贴原文保存为 Markdown，或上传 TXT / MD / PDF。
- **单词**：保存单词、中文释义、例句、来源链接，生成独立 JSON 文件。
- **练习音频**：上传 MP3 / M4A / WAV / OGG / WebM / FLAC / AAC，保留原文件。
- **我的录音**：在浏览器中授权麦克风后录音，停止、试听，再点击保存；也可以上传已有录音。
- 单个上传文件上限 100 MB，浏览器单次录音上限 10 分钟。未点击保存的录音只在内存中，关闭页面前请保存或下载副本。
- 已保存资料可以搜索标题、查看文字、播放音频、下载原文件。
- 「打包备份」把已保存资料、SQLite 一致性快照与当前浏览器的学习进度快照打成 ZIP，保存在资料目录的 backups 中，也可下载副本。当前未保存的草稿不在备份中。

## 启动

在项目目录执行：

```sh
npm run build:cloudstudio
npm run start:local
```

随后打开 http://localhost:3012/ 。本地服务只监听本机 127.0.0.1。电脑关机或退出服务后暂时无法访问，但资料仍在磁盘中。静态托管版本不提供电脑文件读写能力。

双击项目中的 `启动雅思工作台.command` 也可启动已构建的网站（需要已安装 Node.js 和 npm）。

## 默认 legacy 目录的文件组织

```text
ielts-study-library/
  workbench.sqlite3
  manifest.json
  README.md
  articles/<UUID>/metadata.json + article.md 或 original.pdf
  vocabulary/<UUID>/metadata.json + word.json
  audio/<UUID>/metadata.json + original.mp3 等
  recordings/<UUID>/metadata.json + original.webm 等
  progress/<时间戳-UUID>.json
  backups/<UUID>.zip
```

默认 legacy 模式继续从每份资料的 metadata.json 读取；同名标题不会覆盖已有资料。显式开启的 SQLite 模式以数据库作为目录事实来源，不写 metadata.json；其启动与恢复方法见下方。一次保存只有文件和元信息都写完后才进入资料列表。

停止资料服务后执行 `npm run db:init` 可以重复初始化或升级数据库，不会导入、移动或删除已有资料。数据库结构、关系图和分阶段迁移方式见 `DATABASE-DESIGN.md`。

## legacy 目录迁移与恢复

停止本地服务，整体复制资料目录或解压备份中的 `ielts-study-library` 文件夹到新位置。ZIP 备份会通过 SQLite backup API 生成一致的数据库快照，而不是直接复制可能仍有 WAL 写入的数据库文件。启动时用环境变量指定目录：

```sh
IELTS_LIBRARY_DIR="/新的路径/ielts-study-library" npm run start:local
```

学习进度可以在「目标与备份」中导入 progress 下的 JSON 快照。资料库本身按目录直接读取，不需要逐份重新上传。

后续可以让云盘同步整个资料目录；需要云对象存储时替换服务端存储实现，页面通过同一套 `/api/library` 接口访问。不要同时在多台电脑向同一云盘同步目录写入。

## 验证范围

`npm run test:library` 使用隔离临时目录验证四类资料落盘、重新启动读取、音频范围请求、下载、输入与跨站请求拒绝，以及 ZIP 内容和 CRC。麦克风权限和真实录音需要在用户浏览器中使用时授予；开发检查不会开启用户麦克风。

## 在工作台里练习与保存网页

左侧「练习中心」提供听力、阅读、写作和口语练习。内置内容明确标为原创模拟练习；听力音频为本机合成语音，非官方试题录音。无需外链即可阅读、播放音频、答题、查看客观题答案、写作和录音。

- 粘贴公开 HTTPS 文章或直接音频链接，点「读取并保存」。正文或音频先保存到电脑，再在练习中心打开。
- 网页中的音频链接会单独列出，可以点击读取、保存并加入当前材料。
- 导入的是纯文字正文；不会执行网站脚本，图片、交互题目、动态播放器和自动批改不会被复制。登录、付费、反爬或仅动态呈现的页面可能无法读取；网页/链接导入上限25MB，正文上限20万字符。上传入口仍保留。
- 选中原文中的短语可收藏；释义可留待补充，不会伪造字典释义。
- 听力、阅读和写作材料在正文前提供默认折叠的词汇预习区。当前内置国际政治与经济金融常用 B2/C1 词汇，提供音标、四种界面语言释义、浏览器发音、收藏与认识状态；在线词典通过 provider 接口后续扩展。
- 「保存练习与材料」将原文、题目、作答、笔记及录音引用保存为 articles/<UUID>/practice.json。内置听力的WAV会一并复制到audio目录。练习档案可在练习中心再次打开。
- 写作提供字数统计和自查提示，口语提供录音回听；尚未接入AI评分。
- 资源浏览器默认提供「国际政治」和「经济金融」主题。听力与阅读通过 Simple English Wikipedia 的公开分类 API 更新，本机缓存位于资料目录的 `resource-catalog.json`，有效期6小时；网络失败时继续显示上次缓存或内置资源。
- 写作和口语提供同主题的原创轮换材料。完成并保存的资源标识保存在浏览器 `ielts-used-resources-v1` 中，之后从推荐列表移除；清除浏览器网站数据会清除这份过滤记录。
- 公开文章用于听力时，可以使用浏览器自带英文朗读；这类朗读不是保存到磁盘的音频文件，保存练习时会保留文章、作答和笔记。

## 学习事件与周报

「学习记录」顶部显示最近7天报表，包括学习分钟、文章数、新增单词、复习卡、实际听音时间、实际录音时间和认识单词数。单词轨迹记录查看、收藏、标记认识及重新标记陌生的日期。

新增事件保存在浏览器进度 JSON 的 `activities` 数组，并带有 `moduleId` 与 `subjectId`。旧备份没有该数组时会自动迁移为空数组，不影响原有学习记录。听音和录音统计从本版本启用后开始累计，无法从旧音频文件准确反推历史时长。

## 间隔复习

左侧「复习」会把资料库中的单词，以及已保存练习中的客观题自动整理成复习卡。查看答案后可选择忘记、困难、记得或熟练，工作台据此安排下一次日期，并统计掌握数、连续天数、近14天复习量和记住率。

复习卡、安排日期和每次评分保存在浏览器进度中，会随进度 JSON 和资料库 ZIP 中的进度快照一起导出。原始单词和练习档案仍在磁盘资料目录内，迁移时应同时保留资料库和进度备份。

## 快速摸底与界面语言

「能力摸底」包含8道选择题，约10分钟完成。结果是听力、阅读、词汇与语法的基础能力估算，不是 IELTS 官方成绩，也不包含写作和口语评分。每次结果都会写入浏览器进度并随 JSON 备份导出。

顶栏可切换日本語、English、简体中文和繁體中文。首次打开默认日语，语言偏好单独保存在当前浏览器；它不会改变练习题所需的英文原文。


## DEV-001 / DEV-002：隔离 SQLite 目录

旧默认目录及 `metadata.json` 模式保持不变，不回填、不双写、不自动切换。SQLite 模式已接入现有粘贴、文件上传、词汇、公开链接、音频、录音与练习档案。网页导入继续沿用已有格式/大小限制；没有新增题库、EPUB 或 AI 能力。

本次实测 Node.js 24.5.0 与本机 Python 3。启动脚本显式启用 Node 类型剥离以复用同一份进度验证；目录所有权使用 Python `fcntl.flock`（macOS/Linux 本机），不承诺 Windows。首次新目录必须为空，数据库必须是目录内 `workbench.sqlite3`；重启前先停止原本地服务：

```sh
npm run build:cloudstudio
IELTS_LIBRARY_MODE=sqlite \
IELTS_LIBRARY_DIR=/Users/ouki/Documents/Codex/ielts-dev001-preview \
IELTS_DB_PATH=/Users/ouki/Documents/Codex/ielts-dev001-preview/workbench.sqlite3 \
PORT=3013 npm run start:local
```

打开 http://localhost:3013 后在资料库保存内容或上传文件，从列表重开。练习中心可打开已保存材料、保存作答和录音；同一材料的两次练习是不同资料 ID。相同请求重试复用操作 ID 与附件，用户修改内容或开始另一份保存则产生新操作。显示类型与物理文件路径分开，旧 `kind/id` URL 仍按稳定 ID 解析新目录文件。

`.library-mode.json` 固定目录模式；已有 legacy 目录不能改为 sqlite，SQLite 目录不能以 legacy 模式打开。服务、`db:init` 和离线备份竞争同一个 OS 锁。`.writer.lock` 文件保留在目录中，是否占用由 OS 锁判断，不能靠删除文件“解锁”。服务崩溃会释放其文件描述符；若备份子进程仍工作，其继承描述符继续保护目录，直到最后一个描述符关闭。第二个写服务或活跃服务期间独立 `db:init` 会被拒绝。

保存沿用待完成操作→暂存/校验原文件→最终文件→数据库事务的恢复协议。只有两端完成才返回成功。启动清理未提交操作对应文件，保留可重试操作 ID；已提交资料不会被清理。原文件缺失时详情保留目录记录并提示修复。这里验证的是进程中断，不承诺整机断电、外部手改文件或跨电脑云盘同步具有事务性。

### SQLite 备份与恢复

「打包备份」先等待已接收写入完成，再阻止新的写入；读取继续可用。备份期间保存会返回可重试提示并保留表单。后续存储层修改必须复用 `server.withLibraryWrite` / 同一个请求写入协调器；不允许绕过它直接改数据库或文件。

ZIP 内含 SQLite backup API 快照、数据库文件关系指定的原文件、模式标记、`backup-manifest.json` 和本次当前浏览器已保存进度 JSON。清单记录 schema 版本、资料 ID、文件大小/SHA-256、进度文件名与捕获时间。坏文件、缺失文件、无法解析的明确本地资产引用会使备份失败，不提供成功 ZIP。临时文件在完成检查后才改为 ZIP；异常退出遗留 `.partial` 不是可恢复备份。

来源描述 `resourceRef` 区分 `local-asset`、`builtin`、`external`、`vocabulary`、`legacy`，旧 `resourceId` 原值保留。只有明确本地资产引用按目录校验；内置材料、外部来源、词汇活动键原样保留。旧记录不猜测 UUID 或 URL 的所有权，不能确定的引用显示未解析。`resource_tombstones` 目前为空，为后续明确删除状态预留；本任务不提供删除 UI。

备份只包含点击时当前浏览器的已保存进度，不涵盖其他浏览器、之后发生的进度变化或未保存草稿。恢复分三步：

1. 停止目标目录的服务，用脚本校验 ZIP 路径、清单、数据库与文件哈希，并恢复到**尚不存在**的新目录，拒绝覆盖旧资料：

   ```sh
   python3 scripts/restore-library.py /完整路径/备份.zip /完整路径/新恢复目录
   ```

2. 用新目录同时设置 `IELTS_LIBRARY_DIR` 和 `IELTS_DB_PATH`，以 `IELTS_LIBRARY_MODE=sqlite` 启动。脚本会输出清单指定的进度相对路径。
3. 在空测试浏览器配置/新的独立浏览器配置中打开工作台，进入「目标与备份」，明确导入恢复目录中该进度 JSON。重新打开卡片来源、练习历史及音频。仅解包不会恢复浏览器进度。

离线备份需先停止服务，再运行：

```sh
python3 scripts/backup-library.py /完整路径/资料目录 /完整路径/备份.zip --progress progress/要附带的快照.json
```

离线脚本不主动读取浏览器；没有 `--progress` 时不会声称包含当前学习进度。在线备份只能由应用协调器传递持锁描述符启动。

### 验证与回退

`npm run test:library:sqlite` 覆盖原始文本、迁移及中断窗口；`npm run test:library:integration` 覆盖全部保存入口、稳定练习/附件重试、混合资料与五类引用的备份恢复、恢复后进度解析/来源/音频范围读取、锁中写入/合成修改删除、坏文件/子进程失败与父子持锁；`npm run test:library` 保留旧模式文件和 ZIP 回归。所有数据和数据库路径都是隔离临时位置。合成修改/删除仅验证协调器，不代表编辑/删除产品界面已经实现。

自动检查采用合成音频和可控网络返回，不等于真实麦克风、真实在线来源或浏览器逐项点击已验收。必要人工体验是一轮录音回听与备份恢复后显式导入进度。浏览器进度仍在 JSON，不在 SQLite 中。

回退时停止新服务，保留新目录和历史 ZIP，使用旧 legacy 目录启动。源代码副本及差异记录位于本次任务 `work/dev002-before`，DEV-001 副本位于 `work/dev001-before`。迁移 001–007 未改，新增 008；不对真实资料执行破坏性 down migration，不以删除目录作为回滚。此前生成的 README 属于目录内容，不强制覆盖；本说明是当前行为依据。


## DEV-003：资料管理与元数据修正

在 SQLite 模式资料库中选择一份资料，详情显示资料类别、实际文件格式、关联科目和元数据状态。可修改标题、摘要、来源 URL、类别、科目及时长；TXT/Markdown 还可编辑正文。资料类别是展示分组，文件格式由 MIME/原文件决定，改变类别不会移动文件、改变资料 ID 或让 PDF/音频变成可编辑正文。词汇和练习 JSON 保留结构，本任务不开放 JSON 正文编辑。受控标签、体裁、笔记及其他高级分类仍属后续任务。

保存失败会保留表单；有未保存编辑时切换资料/类别或重新提取会提示。保存使用稳定操作 ID 和资料版本号，重试不会重复修改；资料已被另一操作更新时要求重新打开，避免覆盖更新后的内容。标题不能为空，来源只接受有效 HTTP/HTTPS URL，时长是秒数，可留空表示未知。

音频上传默认尝试本机 `ffprobe`，当前自动提取范围是 MP3、M4A、WAV、WebM：读取容器时长和内嵌标题，保留艺术家、专辑、日期和编码等候选 JSON。有标题标签且用户未另填标题时使用内嵌标题；没有标题标签就保留文件名。缺少工具或文件无法识别时仍保留原文件，显示未识别状态，允许手工填写。可通过 `IELTS_FFPROBE_PATH` 指定已有可执行文件位置；不自动安装工具或新增服务。

详情可以重新提取。用户实际修改过的标题/时长记为 `manual`，重新提取保留人工值；新的自动候选及提取时间继续留存。资料更新时间、元数据提取时间与时长修改时间分别显示，不能用一次提取时间冒充人工时长修改时间。新增上传自动提取不会回扫或改写已有真实目录的历史资料。

TXT/Markdown 编辑先写独立的新文件，数据库事务一并切换正文、文件路径、大小与校验值，最后清理旧文件。009 迁移中的编辑操作记录使进程中断后可以保留完整的旧版或新版并安全重试；备份继续只枚举数据库当前文件关系，不把暂存版本当成已保存正文。编辑和重新提取都使用既有写入协调器，备份期间会被拒绝并保留输入。

验证：`npm run test:library:edit` 实测元数据/正文保存重开、四格式各带/不带标签共 8 个音频样例、人工值优先、损坏音频与缺工具降级、4 个异常窗口及 4 个 SIGKILL 编辑窗口、编辑与备份互斥、修改后备份恢复。使用临时合成资料，不操作默认真实目录。现有资料库/备份的 12 项回归也通过。UI 的具体体验结果另记，自动 API 检查不等同于浏览器实际点击验收。

回退：先停止编辑中的服务，保留整个 SQLite 目录及新的备份；源代码修改前副本在本次任务 `work/dev003-before`。001–008 迁移未改，新增 009 前向升级；不要删除新正文文件或手工把数据库路径改回旧文件。需要回到任务前资料状态时，在独立目录恢复任务前备份，不覆盖当前资料。
