# 待办事项

> 产品能力、功能状态和稳定编号统一见 `PRODUCT-CAPABILITY-MAP.md`；本文只保留已有待办背景，不作为产品事实的唯一入口。

## 已确认的产品路线

- 外部产品评估已整理并完成取舍，近期优先顺序为：资料管理基础 → 主观内容进入复习 → 写作/口语结构化自评 → 首页规则建议 → 搜索筛选与里程碑。
- 学习计划和题库在资料关联方式稳定后推进；写作 AI 晚于无费用自评，口语 AI、TTS、Cloudflare 和通用模块继续等待真实需求触发。
- 详细范围、依赖、完成标准和执行计划表见 `PRODUCT-ROADMAP.md`。
- agent 可执行级计划见 `DETAILED-EXECUTION-PLAN.md`；标签规范见 `TAG-TAXONOMY.md`；数据库关系和迁移顺序见 `DATABASE-DESIGN.md`；待用户确认的问题统一见 `DECISIONS-NEEDED.md`。
- SQLite 资料目录地基已建立初版迁移、核心标签 seed 和 `npm run db:init`；当前尚未切换现有页面的数据来源。
- 已确认当前旧资料不需要回填或双写；新资料管理页将直接以 SQLite 为事实来源。
- 第一阶段新增：独立资料管理页、自动提取标题/录音时长、可人工修正、单一 Markdown 备注、多篇 Markdown 笔记、独立修改时间、页面停留/活跃时长及最后查看时间。
- 数据库已预留本地默认用户、学习模块和科目关系；未来会计学、经济学通过新模块和领域扩展表加入，不复制带学科前缀的通用表。
- 资料关系详情、网页离线快照和每 30 天外链健康检查转为后续待办；邮件提醒晚于站内提醒。
- 第一阶段标签判断依据和可靠性分级见 `IMPLEMENTATION-REASONING.md`；口语发音/评分方案与成本见 `SPEAKING-ASSESSMENT-OPTIONS.md`。

## 统一搜索（基础版已完成）

- `Ctrl/⌘ + K` 在任意页面打开搜索，支持键盘上下选择、回车打开和 Esc 关闭。
- 搜索页面入口、学习笔记、复习卡、单词轨迹，以及本机资料标题、来源和可读取正文。
- 音频和录音当前按标题、来源与科目检索；以后有转写文本时自动纳入全文索引。
- 搜索结果可进入对应页面，并直接打开本机资料详情。
- 待增强：持久化增量索引、结果关键词高亮、按科目和类型过滤、音频转写。

## 学习计划（方案已记录，暂未实现）

- 推荐使用“AI 生成 Markdown 初稿 + 工作台验证和排期”的组合，不绑定特定模型。
- 第一版先支持固定格式 Markdown 的导入、预览、任务完成状态和导出。
- 自动顺延、智能重排和 AI 直接生成应在真实使用计划后再决定。
- 数据格式、示例和 provider 边界见 `LEARNING-PLAN.md`。

## 独立导入中心（页面与链接导入已完成）

- 已完成：将练习页的“输入链接并读取保存”迁移到独立导入中心；练习页继续保留资源选择和“打开电脑中保存的材料”。
- 已完成：导入来源索引、链接导入表单、本机保存结果、资料库与练习跳转，以及手机和平板响应式布局。
- 统一支持链接、普通文件、题库和 EPUB；所有来源先转换为带版本的 Study Package，再交给练习、复习、统计和资料库。
- 待实现：带版本的 Study Package，以及 JSON/CSV 题库导入导出、单选、多选、判断和可配置答案的填空题。
- 第二阶段支持 EPUB 目录预览、按章节导入和浏览器即时朗读；第三阶段再接入可缓存的云端 TTS 与 QTI 3 常用子集。
- PDF/DOCX 章节推断、复杂 QTI、AI 生成题目和主观题辅助评分放在后续阶段。
- 详细接口、数据结构、风险和阶段计划见 `IMPORT-CENTER.md`。

## 通用学习工作台（架构已预留，产品范围待讨论）

- 当前 IELTS 保持为第一个 LearningModule；以后可增加语言、考试、专业课程或分阶段学习模块。
- 学习活动已包含 `moduleId` 和 `subjectId`，后续报表可按模块、科目和阶段筛选。
- 内容、词汇、翻译、语法和存储均通过 provider 合约接入，替换服务时不改练习页面的主要交互。
- 后续需要讨论：模块首页、科目结构、阶段目标、不同题型与跨模块复习规则。

## Cloudflare 云端版本（暂缓）

- 使用 Cloudflare Workers 发布工作台和 API。
- 使用 R2 保存文章、听力音频、个人录音与练习附件。
- 使用 D1 保存学习进度、资源使用记录、词汇和复习数据。
- 使用 Cloudflare Access 保护私人学习资料。
- 提供本机进度 JSON 与资料目录到云端的迁移流程。

当前继续使用本机版；在用户明确决定开始云端改造前不部署。

## 练习词汇预习面板（基础版已完成）

- 在听力、阅读正文上方加入默认折叠的重点词汇区域，每篇显示约 8–15 个词。
- 先在本机完成分词、去除常用词和难度排序，再按需查询释义，避免每次打开文章都调用外部服务。
- 合并用户标记为不懂的词；显示音标、发音、语境例句，以及日语、英语、简体中文、繁体中文释义。
- 写作练习提供“本题建议使用词汇”，支持勾选必用词，并检测回答中是否使用；需要处理词形变化。
- 词典结果写入本机缓存。首选 Free Dictionary API，Wiktionary 作为备用；第一版不购买付费 API。
- 风险：公开词典的多语言释义完整度不一致，需要内置常用词表或后续接入翻译/AI 服务补齐。
- 已完成：默认折叠面板、B2/C1 词汇筛选、四语言释义、浏览器发音、收藏与认识状态、写作建议词检测。
- 待增强：通过 provider 接入更大的在线词典、词形还原和更多学科词库。

## 学习统计报表（基础版已完成）

- 增加周报和日期范围筛选：学习总时长、学习天数、练习次数、文章篇数、新增单词数、复习单词数。
- 分别记录听力实际播放秒数和口语实际录音秒数，不能用页面停留时间代替。
- 增加词汇事件记录：首次收藏、查看、复习、标记认识、重新标记陌生；单词详情显示完整日期轨迹。
- “认识的单词”采用可撤销状态，并同时展示累计认识数与本周新认识数。
- 现有学习记录、资料元数据和 Anki 复习事件可用于部分统计；需要升级进度格式并兼容旧备份。
- 文章按唯一资料或来源计数，重复打开不重复计算；重新完成练习仍计入练习次数。
- 已完成：7 天报表、学习/文章/单词/复习统计、实际听音和录音时长、认识状态、单词日期轨迹、旧备份迁移。
- 待增强：自定义日期范围、月报、CSV 导出和按未来学习科目筛选。

## 句子语法解释（较高难度，建议第三阶段）

- 在听力、阅读和写作内容中选中一个句子后，弹出语法卡片：句子成分、从句结构、时态、重点用法、简明释义和例句。
- LanguageTool 可用于发现拼写、语法和风格错误，但不适合解释任意正确句子的完整结构。
- 高质量解释建议调用 AI 模型并要求返回固定 JSON 结构；结果按“句子 + 解释语言”缓存在本机，避免重复费用。
- AI 版本需要用户自己的 API 密钥并会产生按用量计费；实现时应提供关闭云端分析和删除缓存的选项。
- 可先做无 AI 的基础版，只显示规则检查和有限的时态/词性提示，但解释覆盖率和准确性会明显较低。
- 已完成：定义可替换 GrammarProvider 接口和未配置状态；尚未发送任何句子到外部服务。
