# 学习计划与 AI Markdown 导入方案

状态：方案已记录，尚未实现。

## 建议

AI 适合负责“根据目标拆分初稿”，工作台负责“验证、排期、提醒、完成状态和统计”。不要让工作台只能识别某一家 AI 的自由文本，否则模型措辞一变化，任务就可能无法导入。

推荐同时保留两层内容：

1. 普通 Markdown：说明本周目标、安排理由和学习建议，人可以直接阅读和修改。
2. 固定格式的任务清单：每项任务携带日期、科目、时长、数量和资源引用，由工作台解析。

```markdown
---
format: study-plan
version: 1
title: IELTS 第 1 周
start: 2026-09-21
---

## 本周重点
提高阅读定位速度，并保持每天的单词复习。

- [ ] 2026-09-21 | reading | 25m | 完成 1 篇经济类阅读 #article:RESOURCE_ID
- [ ] 2026-09-21 | review | 15m | 完成当天到期单词
- [ ] 2026-09-22 | listening | 20m | 精听并整理 8 个表达
```

导入时先显示预览和错误：无效日期、未知科目、找不到的资料、过长时长和重复任务。用户确认后才写入计划。原始 Markdown 与解析后的任务同时保存，因此可以重新编辑和导出。

## AI 生成时需要的输入

- 当前摸底结果和各项薄弱点。
- 目标分数、考试日期或学习阶段。
- 每周可学习的天数，以及工作日和周末可用时间。
- 希望保持的固定任务，例如每日复习。
- 已保存资料和到期复习量；AI 只能引用工作台提供的资源 ID。

没有这些信息时，工作台应提供普通模板，而不是假装知道用户合理的学习强度。

## 接口边界

- `PlanImportProvider`：读取 Markdown 并生成可验证的任务草稿。
- `PlanGeneratorProvider`：可选；把学习数据和时间约束发送给 AI，返回同一 Markdown 格式。
- 本机版支持手动粘贴或上传 Markdown，不需要 API。
- AI 版本可以从任意模型复制结果后导入，也可以以后配置服务端 API 密钥直接生成。

第一版建议先实现“Markdown 导入、预览、日历任务与完成状态”，使用一段真实计划后，再决定是否加入自动顺延、智能重排和 AI 直接生成。
